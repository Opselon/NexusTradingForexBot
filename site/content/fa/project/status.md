---
title: وضعیت پروژه
description: وضعیت صادقانه هر قابلیت اصلی — تایید‌شده، پیاده‌سازی‌شده، آزمایشی، برنامه‌ریزی‌شده. بدون بازاریابی.
lang: fa
translation-status: complete
source-revision: en:project/status@9.0.6
---

# وضعیت پروژه

> [!IMPORTANT]
> برچسب‌های وضعیت شواهدمحورند. «پیاده‌سازی‌شده» یعنی قابلیت در کد موجود و با
> تست پوشش داده شده. «تایید‌شده» برای قابلیت‌هایی است که سابقه پذیرش جنگل‌بانی
> رسمی دارند. هیچ‌جا ادعای سودآوری تضمین‌شده وجود ندارد.

## تصویر کلی

| بُعد | وضعیت | شواهد |
| :--- | :--- | :--- |
| نسخه | **9.0.6** (semver، منبع یکتا: `pyproject.toml`) | خط لوله انتشار |
| انتشار | **منتشرشده** (تگ‌های v9.0.0 تا v9.0.6؛ SHA-256، مانیفست، SBOM) | GitHub Releases |
| زمان‌اجرا | سخت‌شده؛ PAPER پیش‌فرض؛ LIVE فقط با تأیید صریح | قرارداد CLI + دکتر پیش‌پرواز |
| اجرای زنده | کارا (MT5 Win32 IPC + دروازه ZMQ) با fail-safe تطبیق حساب | رگرسیون BUG-142 |
| حلقه پژوهش | سرتاسری کارا (دیتاست ← walk-forward ← دروازه OOS) | مجموعه‌های research/ |
| قرارداد زنده 50D (`scalp_v1`) | **فعال** | `features/schema.py` |
| قرارداد 70D (`scalp_v3`) | SSoT پژوهشی؛ **فقط کاندید** | `features/schema_contract.py` |
| شواهد 70D تا امروز | walk-forward و سایه روی داده واقعی **منفی / نامشخص** (OOS NOT_ELIGIBLE) | گزارش‌های TASK-05/TASK-09 |
| حکومت چمپیون | RESTORED_CANDIDATE، تصمیم اپراتور در انتظار؛ هیچ ارتقای خودکار | `governance/` |
| CI | ruff · mypy · pytest (~۷۷۹ تست بحرانی) · CodeQL · Trivy | `.github/workflows` |

## محدودیت‌های شناخته‌شده (منتشرشده، نه پنهان)

- سری 70D فقط کاندید با شواهد OOS منفی؛ قرارداد زنده 50D می‌ماند تا کاندیدایی
  همه دروازه‌ها را رد کند **و** اپراتور ارتقا دهد.
- انتشار بسته‌بندی‌شده فقط Windows x64.
- موتور خبر opt-in است (تا افزودن بلوک `news:` در پیکربندی غیرفعال).
- دفتر کامل باگ‌ها (علت ریشه، شواهد، گاردهای رگرسیون) عمومی است:
  [`agents/bugs.md`](https://github.com/Opselon/NexusTradingForexBot/blob/main/agents/bugs.md)
