# tests/tests/unit/test_order_manager_audit.py (subfile documentation)

This test file is documented together with its sibling suite — see
[test_order_manager.md](test_order_manager.md) for the full GUARDS / KEY ASSERTIONS / PITFALLS.

Summary: part of the test_order_manager suite; pins the same subsystem
contract as the sibling tests covered there.
