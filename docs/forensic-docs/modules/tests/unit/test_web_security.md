# tests/unit/test_web_security.py

- **GUARDS / KEY ASSERTIONS / PITFALLS IT ENCODES / NOTES**
- Security & error-hygiene regression tests for the web control surface (DASHBOARD HARDENING contract).
- Guards: an intentional endpoint failure NEVER returns exception text (`test_01_no_exception_text_in_payload`: no RuntimeError, no SECRET_INTERNAL_MARKER_PATH, no `C:/Users` path, no "Traceback"); responses never contain SQL or DB internals (`test_02_no_sql_or_path_in_payload`: no SELECT, audit_ledger, sqlite3, "no such table"); stable error code + message (`INTERNAL_ERROR`); request_id PRESENT and echoed in the header; autogenerated when missing.
- Correlation discipline: every error payload carries request_id (X-Request-ID ↔ body alignment); source audit: no `detail=f"...{err}"` f-string leakage pattern (`re.search(r'detail=f"[^"]*\{[a-z_]*err', src) is None`).
- Fixtures: `_intentional_failure_endpoint`, `boom`, `sql_boom`, `_correlation_middleware`, `failure_client`.
- 35 defs / 377 lines; TestClient-based, no real server.