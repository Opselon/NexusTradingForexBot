# tests/tests/unit/test_bar_aggregator.py (subfile documentation)

This test file is documented together with its sibling suite — see
[test_scalp_features.md](test_scalp_features.md) for the full GUARDS / KEY ASSERTIONS / PITFALLS.

Summary: part of the test_scalp_features suite; pins the same subsystem
contract as the sibling tests covered there.
