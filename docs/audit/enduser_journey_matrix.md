# END-USER JOURNEY MATRIX — ENDUSER-OPERABILITY-HARDENING
### Product under test: shipped v9.0.14 installer + source of this branch

| Journey | Current Behavior | Expected Behavior | Evidence | Failure Mode | User Impact | Required Change | Test Coverage | Status |
|---|---|---|---|---|---|---|---|---|
| DOWNLOAD → INSTALL | Works; per-user, no admin, no dev tooling | — | `install_sandbox.ps1` on v9.0.14 setup.exe → exit 0 | — | none | none | real install executed | OK |
| FIRST LAUNCH (double-click Start Menu) | Console shows `Uvicorn running on 127.0.0.1:8080`; no browser, no tray, no window | Browser opens on the dashboard, or a visible "here is your dashboard" surface | repo-wide `webbrowser` grep = 0 hits (pre-lane); journey.txt | user cannot find the product | **P1** user stuck immediately | `nexus dashboard` + auto-open under TTY/CI guards | `tests/cli/test_cli_enduser_journey.py` (5 dashboard cases) | FIXED |
| Start Menu naming | Shortcuts read `NexusTraderBot`, exe is `NexusScalpEngine.exe` | One product name across exe, shortcuts, Add/Remove Programs | `installer/NexusScalpEngine.iss` [Icons] + `#define MyAppName` | fractured identity; pinning/support search broken | **P1** | **REVERTED — owned by TASK-WINDOWS-UX-001** (taskboard row, IN_PROGRESS; name is deliberate per PR #415). This lane keeps only the shortcut-**target** guard. | `test_eu04_shortcuts_target_the_shipped_executable` | DEFERRED (documented) |
| FIRST RUN DATA | Fresh install opens with 52 tables / 23 signals / 405 news rows from the CI machine | NOT_INITIALIZED databases; a clean first run | `sqlite3 artifacts/audit.db` on v9.0.14 → table_count 52 | stranger's trading history presented as the user's | **P0** | payload hygiene gate before staging | `test_eu01_*` (7 cases, real PS) | FIXED |
| `nexus db status` after fresh install | `DB_MIGRATION_PENDING` on installer-delivered DBs | clean / not-initialized | journey.txt `db status` on v9.0.14 | false migration alarm | **P0** (same root cause as above) | EU-01 hygiene gate | covered by EU-01 suite | FIXED |
| UNINSTALL (normal, checkbox unticked) | Deleted `{app}` wholesale: artifacts/, data/, logs/, archive/ | User data preserved; installer-written files removed by Inno's own log | uninstall probe on v9.0.14 → USER DATA LOST; same probe on rebuilt installer → PRESERVED | irreversible data loss, no recycle bin | **P0** | remove blanket `[UninstallDelete]`; document why | `test_eu02_*` + real install/uninstall both artifacts | FIXED |
| UNINSTALL (explicit "delete my data") | Only path that removes user data | unchanged, explicit | `[Code] CurUninstallStepChanged` | — | — | unchanged | real uninstall probe | OK |
| RESTART after shutdown | Works | — | pidfile + workspace re-anchor | — | — | — | existing lifecycle tests | OK |
| Discover product state | `engine_running: bool` / `runtime_mode: str` / `data_source: str` scattered | one canonical answer: application / engine / mode / trading | `web/server.py:830 get_system_state` | GUI/CLI/API can disagree silently | **P2** | `product_state.derive_product_state` at status --json root | 5-case derivation + CLI journey | FIXED |
| "is live trading possible?" | Conflated with engine_running | `trading` axis: NOT_ARMED / PAPER_ONLY / SHADOW_ONLY / ARMED_LIVE / BLOCKED | as above | accidental LIVE inference | **P2** safety | canonical axis + reasons | derivation cases | FIXED |
| FIND LOGS | `nexus logs` + LOGGING health globbed a path the engine never writes → "no log files yet" | report the real `<workspace>/logs/<severity>/<YYYY>/<MM>/` tree | journey.txt doctor LOGGING WARNING; post-fix: PASS 16 bytes 0d ago | user told they have no logs when reporting a fault | **P1** | `paths.get_engine_log_root()` as single owner | `test_eu05_logs_command_reports_the_engine_log_root` + health check | FIXED |
| OCCUPIED PORT 8080 | `PortOccupiedError`, exit 1, no next action | a clear message + an alternative port / next action | `engine_boot.py:732` | fatal on any 8080 conflict | **P1** | error panel + next action | documented; out of this lane's test budget | DOCUMENTED |
| POST-INSTALL TASKS | health + setup ran concurrently (`nowait`) | sequential: health, then wizard on a verified install | `[Run]` section, pre/post | setup wizard races the health check | **P1** | drop `nowait` | `test_eu02/eu04` guard the .iss | FIXED |
| SUPPORT BUNDLE | no one-command redacted export | `nexus doctor --export-bundle` producing a redacted zip | `release/diagnostics.py` exists, no CLI surface | user cannot ship a useful diagnostic | **P2** | CLI surface (left to the diagnostics lane, documented) | none | DOCUMENTED |
| FIRST-RUN GUIDED SETUP | user must know `nexus setup` | a guided readiness walk to a verified READY state | `cli/wizard.py` | user never reaches READY | **P2** | wizard auto-start (left to wizard lane, documented) | none | DOCUMENTED |
