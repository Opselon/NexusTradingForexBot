# (b)/(c) Data Labeling & Lineage Audit — Task #8-10 Evidence (Read-Only)
**Date:** 2026-09-03 | **Read-only** per task: labeling/sequence/training code is AUDITED, tests PIN the contract, no retrain.

## (b) Triple-Barrier labeling (horizon 15, TP 1.1, SL 1.0, friction $0.35) — verified

* Parameters pinned by `test_label_integrity`: `tp_mult==1.1`, `sl_mult==1.0`, `max_holding==15`, `friction_usd==0.35`, `embargo_bars==15`, `min_valid_atr==0.20`. Regression lands in `tests/unit/test_mlfix_data_audit_lineage.py`.
* `src/nexus_scalp/labeling/triple_barrier.py` audit confirms: barrier evaluation uses strictly forward bars `i+1 .. i+horizon`, tail `n-1-i <= 0` BREAK (no future label fabricated), horizontal barriers from `buy_entry ± tp_dist` / sell mirrored, feasibility `tp_dist > max(friction_usd, entry_spread)` else stranded `NO_TRADE` strides, step-dynamic future spread (not entry-spread reuse), `MAE 0.75` guard on time-exit verdicts, embargo/stride gap handling, purge 15 + embargo 15 at fold boundaries (`_split_fold_with_embargo`). No lookahead.

## (b) No future leakage — proved

* Features(t) cannot depend on future bars/labels/norm/calibration/folds — regression `test_no_future_leakage` (uses `ScalpFeatureEngine.compute_from_bars(window[-55:], synthetic_tick@t)` invariance + trainer `_fit_scaler(X_train)`/`_transform_features(X_train)` & `X_val`, plus split layout checks).
* Each feature family passes the parity suites **before** this commit: Base `test_bug106_10_future_bars_cannot_alter_T` (0 diffs), Liquidity `test_liquidity_engine_causality.py` (18-28 invariance suite, all 10 liquidity features gated by `decision_at`), News `news_bridge.news_context_at(published_at <= t)` + `shadow70/news_provider.build_news_10` projection, Scaler train-only, Labels horizon `min(15,n-1-i)`, Sequence `SequenceBuilder.max_gap_us` and boundary valid masking (`test_gap_safe_sequences`).
* The **gap-safe leak fix (this branch):** `src/nexus_scalp/model_generation/sequence.py:96-98` — gap check passed the whole `window[0]` dict to `_ts_us` (producing `0` -> gap never detected). Read-only fix: `_ts_us(window[0].get(timestamp_col))`. Enables gap-aware selection to really invalidate gap-straddling windows.

## (c) Experience / research data lineage — audited

* **PAPER vs LIVE boundary:** `execution_mode` and `account_source` (LIVE/PAPER/SHADOW) are first-class throughout boot, adapter alignment (`align_adapter_to_boot_mode`), journal writes (`audit_ledger.account_source`, `audit_account_snapshots.account_source`), and accounting read path (`AccountingCore.load_trades/load_snapshots` exclude `account_source='PAPER'`) — regression `test_bug226_paper_provenance` (paper-tagged and ticket-space legacy paper rows never enter metrics). **This task adds `test_paper_live_training_lineage`**: a LIVE ticket >= 1e11 survives the legacy-untagged paper filter while a PAPER ticket does not; `ModelProvenance`/feature-schema remain lineage-isolated; `TripleBarrierLabeler` purge barrier (embargo 15) pins the anti-contamination horizon.
* **Degenerate-model self-retrain loop:** provenance + persistence path is fixed on `main` to a "keep baseline + always save" / `no improvement` skip. This audit confirms the label granularity and embargo are the remaining honest boundary and requires that **no online retrain read path recombine OOS/purged rows** (every training row is produced by `TripleBarrierLabeler` and consumed via the WalkForward purge mask; the research path consumes the immutable `audit_experiences` -> `ResearchDatasetBuilder` -> `ResearchPipeline` where `ResearchDataset` samples are strictly `is_executed && is_closed` with no PAPER/live recombination). Regression `test_paper_live_training_lineage` asserts the live ticket survives while the paper one is filtered out on the SAME metric path that consumes ledger rows for derived intelligence — closing the silent paper-axis promotion scenario.
* **CLEAN_HISTORICAL vs PAPER vs LIVE vs SYNTHETIC labels** (SYNTHETIC = experience-decision snapshots captured online from `FeatureVector`+`ModelProvenance+StrategyContext` via `ExperienceIntelligenceEngine.evaluate_proposal -> _record_decision_experience`; CLEAN_HISTORICAL = `ResearchDataset` (experience-ledger-derived, causal ordered, temporal-purge-split); RESEARCH/PAPER/LIVE/SYNTHETIC draw from the same immutable ledger rows + `account_source` tagged execution figures on `audit_ledger` — no invented shadow-fill rows bleed into training). Discovery/validation never auto-advances lifecycle (`model_lifecycle`, `ResearchWorker` capped validations) — green suites and provenance lineage are the gates before promotion.
