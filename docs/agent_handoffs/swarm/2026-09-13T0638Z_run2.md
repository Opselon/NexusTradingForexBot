# NSE-Swarm run — 2026-09-13T06:38Z — Role 2 (Backend/Domain)

## Defect: BUG-268 — minimum-loss grace gate wall-clock fallback (clock-domain sweep residue)

**STATUS: FIXED-VERIFIED (local; PR pending branch protection)**

## Root cause (file:line at a9d29e21)

The G3 lineage (15d39552 `_arbitrate_decision`, 7b7aeaa2/BUG-259
`_calculate_hold_value_score`, BUG-260 `_update_mfe_mae` +
`live_engine._position_performance`) swept host-wall-clock hold-age
derivations off the exit surface — but the sweep's own claim
("no change on now-is-not-None path", BUG-259 regression covers only the
hold-score method) missed the FOURTH consumer:

`src/nexus_scalp/execution/lifecycle/scoring.py:513-514` (pre-fix,
`_evaluate_minimum_loss_optimization`):

```python
else:
    now_ref = datetime.now(UTC) if entry_time.tzinfo else datetime.now()
    duration_sec = (now_ref - entry_time).total_seconds()
```

This duration gates the 60-second Spread Overcome Grace Period at
`scoring.py:520-521` — the entry condition for the ENTIRE MIN_LOSS exit
cluster (EV breach / deep drawdown / weak recovery). Mixed clock domains:
- host wall clock AHEAD of the tick domain (production-observed direction)
  -> a fresh position reads as >60s old -> MIN_LOSS cluster can fire
  instantly, defeating the "let the trade breathe" guard (money path);
- host BEHIND -> grace stays shut for true grace + skew -> minimum-loss
  exits suppressed (the `Age: -10781.6s` class);
- the naive branch re-opened the mixed naive/aware subtraction crash class
  G3 retired.

## Fix

Parity with the established contract, no new machinery
(scoring.py:489-540 post-fix):
- duration computed ONLY when `now` is provided AND shares the entry
  anchor's tz domain (same `(tzinfo is None) == (now.tzinfo is None)`
  guard as BUG-259);
- otherwise conservative 0.0 (grace stays CLOSED — fail-safe: never an
  unearned exit) + exactly one rate-limited (300s monotonic) loud
  `event=HOLD_AGE_FALLBACK mode=no_tick_timestamp_conservative_zero`
  WARNING;
- manage-loop call site (`order_manager.py:2493` `now=now`) already
  threads the tick stamp — threaded behavior byte-identical;
- unused `UTC` import dropped.

## Evidence (all run this session)

- RED-before (stash probe: fix reverted at same HEAD, test file kept):
  4/7 fail — `test_no_now_conservative_zero_keeps_grace_closed` (90s-old
  entry + no threaded now -> exit FIRES pre-fix on wall clock),
  naive/aware-mismatch, warning-battery, static `datetime.now` guard.
- GREEN-after: `test_bug268_min_loss_grace_clock.py` 7/7.
- Lane: bug268+bug259+bug260+g3+rule_matrix+order_manager_exit_bugs
  = 22 passed RC=0; neighbors exit_separation_w3+trade_lifecycle_task3
  +s4_position_intelligence_golden+lifecycle_bug140 = 102 passed RC=0.
- ruff check + format clean on both files; mypy clean on scoring.py.
- test_critical_suite.py meta 3 passed RC=0 with the new gated entry
  (file `git add`ed in the same commit — rc=5 lesson honored).

## Why not K3 (the planned role-2 item)

Prereq check this run: `ls data/raw` -> ABSENT on this host (gitignored
operator-exported CSVs; the BUG-266 replay tests skip loudly for exactly
this reason). K3 (paper-vs-demo bias job) measures fills on identical
signals across substrates — with zero recorded data on the host there is
nothing to replay, so the bias measurement cannot be validated with
executable evidence here. A K3 job built blind would repeat the
"shipped-but-never-wired" failure shape BUG-266 just closed. Chose the
fully-verifiable P1 defect instead; K3 stays in `second_open` with the
data prereq explicitly recorded as an operator dependency.

## Files

- src/nexus_scalp/execution/lifecycle/scoring.py (fix)
- tests/unit/test_bug268_min_loss_grace_clock.py (new, 7 tests, gated)
- tests/critical_suite.txt (manifest entry)
- agents/bugs.md (BUG-268 row)
- docs/agent_handoffs/swarm/2026-09-13T0638Z_run2.md (this report)
- docs/agent_handoffs/swarm_log.md (one-line summary)
- artifacts/swarm_agent/state.json (+ mirror ~/.nse_swarm_state.json)

## Next (role 3 ML/Training)

1. Check for main-gate red at this run's merge sha (PR); SMOKE-COUNT-WINDOWS
   flake — root-cause on second occurrence (now: watch for recurrence since
   it re-flaked context in #173 window).
2. K3 bias job — BLOCKED on host recorded data (operator: export M1/tick
   CSVs to data/raw on the swarm host, or point paper_data.raw_bars_path at
   the dataset-cache). Unblock next run that has data.
3. Residual hold-clock surface audit: `pending_orders.py:408
   datetime.now(UTC)` is a history-window query bound (not a hold age) —
   accepted; `_update_mfe_mae:4108 now or datetime.now(UTC)` is magnitude-
   only path (entry_time=None skips elapsed stamps) — accepted, pinned by
   bug260 static guard.
