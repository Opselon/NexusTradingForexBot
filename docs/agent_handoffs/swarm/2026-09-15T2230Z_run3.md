# NSE Swarm run — 2026-09-15T22:30Z — role 3 (ML/Training window; executed the in-flight perf-wave handoff)

## Cycle summary

Start state: main `88bb4546` (0/3 behind), pulled clean to `3df969bb`. The previous run's
plan ("verify post-merge gates, then PERF-WAVE R5") was overtaken by events: a sibling
lane had already shipped R5 as `fix/bug297-evaluator-hot-path` (BUG-297, commit 1bd786c5)
with NO PR. This cycle therefore did the PR-adjudication job (skill step 7) for it and
landed it.

## BUG-297 (perf-wave R5) — FIXED-VERIFIED + LANDED-MERGED (PR #222, squash d0764962)

**Defect:** `StrategyEvaluator.get_registered_strategy_score` (evaluator.py:603 pre-fix) —
the pre-trade tier-3 fallback (`intelligence._get_score` <- `run_post_policy_stages` <-
`_process_tick_pipeline`) paid a RAW `sqlite3.connect` per entry-candidate proposal on the
event-loop thread once the <=4/s inline-refresh budget was starved. Second silent defect:
raw connects bypassed `AuditRepository._connect_sqlite`, the single URI-contract connect
site (BUG-156 class — `file::memory:?cache=shared` becomes a literal junk CWD file).

**Fix (lane's design, verified sound):** `_registry_conn(timeout)` seam — one handle cached
per (thread, busy-timeout tier) in `threading.local()`, opened through `_connect_sqlite`;
same-thread guard intact (no `check_same_thread=False`, no shared lock); repo-swap/path
staleness drops handles; statement failure evicts + re-raises into the caller's existing
except path (return shapes byte-identical, never raises into the tick loop); closed-repo
delegation = exact pre-fix one-shot behavior; `LiveEngine` shutdown calls
`evaluator.close()` before `audit.close()`. Honest scope note accepted: the indexed PK
SELECT itself stays synchronous at tier 3 (churn removed, not the read; the TTL'd
in-memory registry copy remains a follow-up).

## Independent verification at 1bd786c5 (worktree /tmp/nexus-bug297, this run)

- New gated battery `tests/unit/test_bug297_evaluator_hot_path_reads.py`: **17/17 pass**.
- Neighbors + battery: **144/144 pass, RC=0** (experience_intelligence,
  provenance_contract, integration experience_execution_boundary,
  audit_repository_memory_uri_bug156, bug292_spread_sketch, bug297).
- `ruff check` + `ruff format --check` clean on touched files; `mypy` Success.
- `verify_critical_suite_manifest.py`: 165 paths all exist; battery registered.
- Merge-base: PR based on current main tip `3df969bb`; diff touches no `.github/workflows/*`.

## CI-gotcha found (NEW OPERATOR-INVISIBLE DEFECT CLASS, worked around)

PR #222 was created 21:45:51Z and got **ZERO workflow runs** — the `pull_request` opened
webhook event was silently dropped (repo-wide run stream shows nothing between the
21:45:11Z main-push runs and the 21:57:27Z sibling re-push; required checks can never be
satisfied, PR hangs "no checks reported"). An empty-commit push at 21:58 ALSO produced no
runs for ~10 min; only a close+reopen at ~22:13 forced the events (runs at 22:13:58Z, all
15 checks green incl. windows+macOS legs in <6 min). Same window hit sibling lane #221
(their 21:57Z forced re-push is visible in the run stream). This matches the known
intermittent delivery-degradation shape on this host/account (DNS incident precedent);
it is transient, not a repo-config fault (no webhooks, Actions fully enabled).
**Playbook for future runs: if `gh pr checks` says "no checks reported" for >5 min, do NOT
wait — close+reopen the PR, then re-poll.**

## Landing

- `gh pr merge 222 --squash` → main `d0764962` at 22:24:28Z; PR state MERGED; remote branch
  auto-deleted by squash; local worktree removed, local branch deleted; shared tree clean
  at origin/main.
- Post-merge gates at d0764962 at report time: CI Integrity / DepDrift / Migration safety /
  DepManifest / Docs / JS / OSV / Trivy / Pages = success; **Code Quality & Tests, CodeQL,
  Py Tests (win), Py Tests (mac) still in_progress** → next run confirms (same shape as the
  BUG-292 tail; that one went 16/16 green).

## Perf-wave status after this run

R1/R2/R6/R7/R8/R9 = landed (BUG-287/285/292/290/291/294). **R5 = CLOSED this run (BUG-297,
#222).** Remaining: R4 (sync MT5/gateway RPC ~3s on runtime_loop.py:376 — needs a design
note, touches execution boundary), R10 (news.db ~9.5MB/day retention — role 5/6; any
migration MUST bump manifest schema versions in the same commit), plus R5-follow-up
(TTL'd in-memory registry copy, cross-lane design).

## Other ledger notes

- #292 post-merge gates at fa34fccd: **16/16 success confirmed this run** (incl.
  windows + macOS Py Tests). BUG-292 fully accepted, no nightly-watch caveat remains.
- K3 data/raw re-check: ABSENT (19th time) — operator export still the prereq.
- Open PRs at cut: #221 (BUG-296 zero-state bootstrap, checks were green at 21:57Z head
  24bbcb40 — sibling lanes own it), #218 already merged this window (1d647859).
- Operator TODOs unchanged (NSE_UPDATE_SIGNING_KEY, TELEGRAM-SEND-FAILED secret,
  nightly-qa CI_RESULTS_DIR, nightly-e2e curl -f, js-tests.yml python deps, data/raw,
  docker rebuild, BUG-271 installs, poisoned live audit.db repair + VACUUM).

## Next-run plan (role 4 Research/Backtest per rotation)

1. FIRST: confirm post-merge gates green at `d0764962` (Code Quality & Tests, CodeQL,
   win/mac Py Tests were in_progress at cut) — if any red, adjudicate per skill step 7.
2. Then pick the top P1/P2 for role 4: the research/backtest wave items or PERF R10
   (news.db retention) — R10 is likely the cheapest concrete win and has an established
   pattern (dead_letter_store batch + news_prune_audit bookkeeping + manifest bump).
3. If a PR sits >5 min with "no checks reported": close+reopen playbook (this run's
   discovery), never wait on the dropped webhook.
