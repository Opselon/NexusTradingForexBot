# NSE-Swarm run 2026-09-11T2230Z — role 6 Runtime/Linux — BUG-259 FIXED

BUG-259: hold-score Time-in-Loss penalty (up to -30) derived holding_duration
from the HOST WALL CLOCK (datetime.now, scoring.py:412-421 pre-fix) instead of
the tick-threaded now the manage loop already threads everywhere else
(order_manager.py:2687) — G3 clock-domain residue. Host-behind-broker skew fired
spurious -30 on fresh positions; opposite skew suppressed it forever.

FIX: now threaded manage-loop -> _evaluate_hold_score -> _calculate_hold_value_score
; duration from tick domain only (same naive/aware domain required); no-now ->
conservative 0.0 + rate-limited HOLD_AGE_FALLBACK warning (G3 pattern). Delegate
passthrough unchanged.

EVIDENCE: new tests/unit/test_bug259_hold_score_hold_clock.py 7/7 (RED 7/7
pre-fix via stash probe at af2bf200); 95 passed across 12-suite hold-score lane;
ruff check+format clean (1030 files); mypy clean on touched files.

BONUS (pre-existing, baseline-compared): test_rule_matrix::test_api_endpoints
401-flake fixed (NSE_WEB_AUTH_DISABLE fixture opt-out);
test_exit_separation_w3 flip test maintenance-window flake fixed (tick pinned
mid-session, a15 class); rule_matrix hold-score test re-pinned to explicit tick
stamp.

LANDED: agents/bugs.md BUG-259 record (commit 0cac59dc on
swarm/bug259-hold-score-clock); code+tests commit pending PR (branch
protection). Report: artifacts/swarm_agent/reports/2026-09-11T2230Z_run6.md.
NEXT: PR + merge, then role 7 (QA) — candidates: G3-residual sweep for any
remaining datetime.now consumers on tick-path scoring; UPDATE-SIG-OPERATOR
still parked on operator.
