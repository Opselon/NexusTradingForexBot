# NSE Swarm Run — 2026-09-14T0400Z — role 6 (Runtime/Linux)

## Items worked
1. **BUG-274 (P1) FIXED — bound-seam wrapper-state leak CLASS (13 sites).**
2. **BUG-275 (P2) FIXED — DB-hygiene cadence clock-domain mix.**
3. NIGHTLY-E2E ledger: re-checked — no scheduled run since 09-13 07:36Z at run end (02:25–04:00Z window); the ~07:30Z occurrence will be the first real test of the dcc57826 PG seed. Stays OPEN-MONITOR.
4. K3: data/raw still ABSENT (12th consecutive check).

## BUG-274 — root cause
The P1/L5–L8/S10 god-file decomposition moved method bodies VERBATIM out of
LiveEngine/OrderLifecycleManager into BOUND wrappers (`BarHandler(om)`,
`RuntimeLoop(om)`, `TickPipeline(om)`, `ProtectionEngine(om)`) whose only
state surface is `self.om`. Pre-extraction `getattr(self, "X")` reads became
permanent-default reads post-move. BUG-169/md7 already proved this class on
the duplicate-tick guard — and fixed ONLY that site. A repo-wide AST sweep
(wrapper classes assigning `self.om = om`, reads never assigned in-file) found
the 13 siblings:

| site | attribute | impact |
|---|---|---|
| runtime_loop.py:328/393 | `_last_account_refresh`, `_last_snapshot_refresh` | **PERF-02 5s throttles NEVER engaged** — per-tick gateway RPC (`get_account_info` + `get_account_snapshot` + `_update_runtime_mode`) the throttle exists to eliminate, on the tick path |
| runtime_loop.py:338/375 | `_last_account_info` | failure-fallback + cache-hit always None → loop `continue`-sleeps instead of serving cache |
| bar_handler.py:214/302 | width-split CRITICAL / DISABLED_BY_CONFIG stamps | hourly rate gates short-circuit OPEN → **log per M1 bar** (EXEC_TRACE class) |
| bar_handler.py:210 | `_online_train_disabled` | boot self-disable invisible to the bar path |
| bar_handler.py:40/175 | `candle_intel`, `mslie_engine` | subsystems unreachable even when operator-enabled |
| bar_handler.py:140/162 | `news_engine`, `_last_proposal` | radar `news_state`/`decision_reason` permanently null |
| tick_pipeline.py:308 | `server_state` | live SMC chart-overlay refresh never runs from the tick path |
| protection.py:721 | `_last_tick_for_ticket` | VOLATILITY_EXPANSION breakeven-breach check can never see a price → protective market-close branch dead |

The companion test `test_perf02_account_refresh_throttle.py` stayed green the
whole time because it pins a hand-copied MIRROR on a `_FakeEngine` that
re-creates the wrapper-local pattern — mirror tests cannot catch seam drift
(captured in the fix's docstring; mirror left in place, real pins added).

## Fix
Every leak re-pointed at `self.om.X` (the surface the writers actually set —
assignments verified at live_engine.py:956/1281/1320/1486/676-677/1500,
order_manager.py:641 + tracker property). `_a16_last_trainer_width` stays
intentionally wrapper-local (assigned in-method; pinned so).

## Tests (RED-before proven in a clean baseline worktree at fd95a7b0)
- NEW `tests/unit/test_bug274_wrapper_state_leak.py` — 10 tests, critical_suite-gated: RED 10/10 pre-fix, 10/10 post. Behavior pins (hourly-once CRITICAL/INFO across 3 bars + stamp lands on engine; self-disable early-return; radar fields flow; MSLIE feed runs) + md7-style source pins + **class-wide AST guard over 6 bound seams** rejecting any future `getattr(self, X)` leak.
- NEW `tests/unit/test_bug275_hygiene_cadence_clock.py` — 5 tests, gated: RED 4/5 pre-fix (permanent-due proof), 5/5 post.
- Lanes RC=0: bug274+bug275+oplog-hygiene+storage-wiring 40; agent16+radar+perf02+ag13+regime+bug212+agent8+om-exit-bugs (baseline-proven: test_market_radar_integration reds are PRE-EXISTING host-state — P0-2 trust anchor refuses this sandbox's stale on-disk model.pt, identical at fd95a7b0); s6-tracker+sentinel+bug271+storage+calendar+bug185 57; hygiene+maintenance family 36; critical_suite meta green.
- uvx ruff@0.16.6 check + format --check repo-wide clean (1989 files); mypy Success on 5 touched src files.

## BUG-275 — root cause + fix
`MaintenanceCycle` passes `now_t = time.time()` into `is_deep_due`/
`is_telegram_due`, but the scheduler stamped `_last_light/_last_deep` with
`time.monotonic()` (~uptime) — wall(1.79e9) − monotonic(1e5) ≥ 6h is
ALWAYS true: deep index-health scans every 30-min light cycle (12× intended
IO) + nonsense `next_light_in_sec` on CLI/web panels. Fix: wall-domain
contract for all cadence stamps/comparisons (duration measurement stays
monotonic). 4 sites in hygiene_runtime.py.

## Files
- src/nexus_scalp/application/live/{bar_handler,runtime_loop,tick_pipeline}.py
- src/nexus_scalp/execution/lifecycle/protection.py
- src/nexus_scalp/hygiene/hygiene_runtime.py
- tests/unit/test_bug274_wrapper_state_leak.py (new, gated)
- tests/unit/test_bug275_hygiene_cadence_clock.py (new, gated)
- tests/critical_suite.txt (+2 entries), agents/bugs.md (BUG-274/275 rows)

## Next (role 7 QA/Tests)
- Closeout: PR + merge + post-merge gate verification.
- NIGHTLY-E2E verdict from the 09-14 ~07:30Z run.
- Candidate: MIRROR-test audit — other hand-copied logic mirrors (test_perf02
  shape) that can mask seam drift; the BUG-274 class guard could extend to
  src/ execution/lifecycle wrappers.
- Throttle-sentinel class: dead_letter_store + storage/runtime already
  None-sentinel; sweep COMPLETE (bug274 AST guard covers bound seams).
