# NSE-Swarm Run Report — role 5 (Data/Ingestion) — 2026-09-16T13:24Z

## Defect: BUG-302 — BYTE_IDENTICAL_DUPLICATE hygiene detector for news_articles

**Status: FIXED-VERIFIED + LANDED-MERGED — PR #241 squash `1c6f9337`, 17/17 checks green.**

### Root cause (file:line at e32f6f42)

- `src/nexus_scalp/hygiene/detectors.py:141-214` — `scan_news()` only detected two duplication modes: (1) legacy `is_duplicate=1` flagged rows with verified canonical via `duplicate_of` hash, and (2) `news_analysis` duplicate `(article_id, run_id)` tuples. **Blind to the dominant production mode**: byte-identical re-ingest churn from hash instability (BUG-282 RFC-822 pubDate fabrication → per-poll re-identity minting). Lane-04 measured **26,161 byte-identical rows / ~80 MB payload + ~9 MB index share** (`docs/audit/wave_20260914/04_db_forensics.md:70`) — the detector reported **3 duplicates** while forensics found **26,161** (`04_db_forensics.md:80`). Hygiene's initial report showed `duplicates_found 3, retention_candidates 0, delete_candidates 0, rows_scanned 0` while the real dup count was 26,161.

### Fix (3 src files, no schema changes)

1. **`src/nexus_scalp/hygiene/detectors.py:186-255`** — Added `BYTE_IDENTICAL_DUPLICATE` detection path in `scan_news()`: SQL window function `FIRST_VALUE(article_id) OVER (PARTITION BY canonical_url, title, body, summary ORDER BY rowid ASC)` identifies the canonical row (earliest rowid), `ROW_NUMBER()` marks subsequent identical rows as duplicates. Robust to missing columns (PRAGMA-guarded), skips empty url/title, de-duplicates against `is_duplicate=1` set to avoid double-reporting.

2. **`src/nexus_scalp/hygiene/worker.py:46-47, 680-692, 501-502`** — Registered `"BYTE_IDENTICAL_DUPLICATE"` in `SAFE_CLEAN_CLASSES`; wired `_canonical_exists()` to verify canonical row via `article_id` lookup (not `article_hash`); updated `_table_rows_sql()` to `SELECT *` for full row archival.

3. **`tests/unit/test_byte_identical_news_dedup.py` (NEW, 6 tests, critical_suite-pinned)** — Unit detector test (`test_detector_finds_byte_identical_duplicates`), allowlist test (`test_byte_identical_class_registered_safe`), DRY_RUN blocking (`test_dry_run_detects_but_does_not_delete`), SAFE_CLEAN archive+delete (`test_safe_clean_archives_and_deletes`), canonical-missing safety (`test_canonical_missing_blocks_delete`), missing-column graceful degradation (`test_missing_columns_graceful`). All 6 GREEN. Existing hygiene suite (35 tests) GREEN: `test_database_hygiene_task11` (11), `test_bug295_hygiene_dead_rules` (10), `test_bug275_hygiene_cadence_clock` (5), `test_storage_lifecycle_wiring` (7), `test_observability_guardrails` (46).

### Regression net

`tests/unit/test_byte_identical_news_dedup.py` — source-pin parity (detector ↔ executor) via `cleanup_class` propagation, mirroring BUG-292 pattern. No threshold weakened; BYTE_IDENTICAL_DUPLICATE is a NEW safe class, additive to existing `DUPLICATE_WITH_CANONICAL`.

### Implication for the program

Operator-gated historical cleanup now possible: `nexus hygiene --mode SAFE_CLEAN --apply --database news` will archive+delete the 26,161 redundant rows (~89 MB recovered per cleanup report). The triple-copy elimination (body==summary 28,431 rows + analysis.summary==article.summary 25,011 rows = ~94 MB) is a SEPARATE SAFE class (`STALE_DERIVATIVE_VERSION`) not yet scoped — tracked in `PERF-WAVE-R-OPEN` R10 remainder.

### Landing

`623e481b` (BUG-302) + `f5e368b0` (ruff-format fixup, same PR) → PR #241 → squash-merged as `1c6f9337`. Landing preflight caught the wave-5 commit shipping unformatted files: repo-wide `ruff format --check .` reported 3 files would be reformatted, which would have failed the CI `ruff_format` gate. Fixed in the fixup commit before the PR was opened — never rely on the touched-files check, run it repo-wide.

### Next run

Role 6 (Runtime/Linux).