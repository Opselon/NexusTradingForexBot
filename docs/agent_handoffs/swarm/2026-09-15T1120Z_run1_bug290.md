# NSE-Swarm Run Report — role 1 (Architect) — 2026-09-15T11:20Z

## Item
BUG-290 / BUG-291 — perf-wave R7 + R8 (+ R9 stamp map): three unbounded, never-evicted
in-process guard maps on long-lived paths. Source: docs/audit/wave_20260914/10_performance.md:128-129
(queue explicitly assigned to "role 1 pick if nightly acceptance is green" — nightly IS green,
all main gates success at 2e53fd04).

## Root cause (file:line at HEAD 2e53fd04)
1. `src/nexus_scalp/execution/order_manager.py:510` — `self._processed_orders: dict[str, bool] = {}`
   written once per dispatch attempt at `execution/lifecycle/dispatch.py:144` (hedge), `:512` (market),
   `:586` (pending); read at `dispatch.py:100/:361/:365`. Grep over `src/` proved ZERO prune/eviction/
   clear sites (only consumers besides dispatch: `web/debug_snapshot.py:1124` len()). Unbounded for the
   process life.
2. `src/nexus_scalp/execution/order_manager.py:513` — `_ai_flip_warn_times` docstring (`:1702-1703`)
   claims the stamp is "cleaned up with the ticket"; `grep _ai_flip_warn_times.pop` = no matches.
   Dead cleanup claim, never bounded.
3. `src/nexus_scalp/accounting/core.py:90` — `_report_cache: dict[str, PeriodReport] = {}`, written at
   `:528` keyed `{kind}:{bounds.key}`; read at `:481`/`:522`. The worker warms ~57 keys/cycle
   (`_SERIES_COUNT` 30/12/12/3) but `period_series` caches whatever moments callers pass
   (`web/debug_research_routes.py:1113` accepts count<=60 per kind) — monotonic growth.

## Fix
New single-owner seam `src/nexus_scalp/bounded_map.py::BoundedLRUMap`:
- stdlib-only (AST-pinned so it can never gain an app import; import-cycle + hot-path weight guard)
- `MutableMapping` surface → existing dict-shaped stubs/tests (test_maintenance_dispatch_guard:65,
  test_runtime_failure_injection:148, test_debug_snapshot_phase20:120) keep swapping plain dicts
- O(1) ops; `__contains__` deliberately does NOT touch order: the per-tick guard lookup
  (`in self.om._processed_orders`) stays exactly as cheap as a dict lookup (INV-001)
- insertion-capped, LRU-first eviction, `evictions` counter surfaced in `debug_snapshot`
  (`processed_orders_evictions`, None-safe on plain-dict stubs)

Wiring: `_processed_orders` cap 50_000 (`PROCESSED_ORDERS_GUARD_MAX`), `_ai_flip_warn_times`
cap 10_000, `_report_cache` cap 512 (`REPORT_CACHE_MAX`) — each with the safety argument recorded
in-source.

## Dedup-safety argument (why LRU is not a duplicate-order hole)
The in-memory guard is a SAME-SESSION pre-broker filter. Cross-boot protection is durable:
`adapters/database/executions_idempotency.py` (UNIQUE `idx_executions_order_status`, BUG-254
lineage) + `tests/unit/test_recon_duplicate_deal_across_restart.py` proves the ledger-side
collapse. policy request_ids are per-tick; evicting an entry older than 50k dispatches cannot
re-open a realistic same-boot replay. (Recorded in the constants block at order_manager.py:182.)

## Verification (all executed, no claims from messages)
- RED-BEFORE: worktree at 2e53fd04 + battery → collection ERROR `No module named 'nexus_scalp.bounded_map'`
  (the fix module's absence IS the red-before state for an infrastructure seam).
- GREEN: `tests/unit/test_bug290_bounded_guard_maps.py` 14/14 (RC=0, slim venv).
- Neighbors RC=0: test_execution_quality_contract + test_maintenance_dispatch_guard +
  test_runtime_failure_injection + test_capital_protection_a15 + test_agent12_execution_forensic (79 tests);
  test_accounting_core + test_accounting_advanced_metrics + test_accounting_deduplication (101 tests).
- test_debug_snapshot_phase20: 6 TestDebugApi reds — BASELINE-PROVEN identical at 2e53fd04 pristine
  worktree (pre-existing host web-auth shape, disclosed since the BUG-285 wave). The file's own
  `_processed_orders` pin (test_debug_15_execution_trace) passes.
- Engine import smoke: `import nexus_scalp.application.live_engine` + maintenance OK.
- ruff check/format clean repo-wide (2040 files already formatted); mypy clean on all four touched
  src files. critical_suite manifest: 356 lines, 0 missing paths; new battery registered.

## Behavior deltas
- dispatch duplicate-guard semantics unchanged for anything in the working set; only >50k-stale
  ids can be forgotten (documented, durable layer refuses anyway).
- `list(m)` iteration order is LRU (oldest→newest) not insertion — no production consumer iterates
  these maps (grep-proven: only `in`, `[]`, `.get`, `len`).

## Ledger
- PERF-WAVE-R-OPEN queue now: R3 (redesign), R4 (sync MT5 RPC), R5 (tier-3 sqlite), R6 (spread-percentile
  SELECT in policy), R10 (news.db retention). R7/R8/R9 = CLOSED here.
- Next: role 2 Backend/Domain — R6 or the HEALTH-WIRING split PR (decision record 2026-09-15).
