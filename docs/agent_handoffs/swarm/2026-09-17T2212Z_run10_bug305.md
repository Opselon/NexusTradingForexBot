# NSE-Swarm run10 — Role 10 Accounting/Forensics — BUG-305

- Run id: `2026-09-17T2212Z_run10_bug305`
- Role: 10 (Accounting/Forensics)
- Worktree: `/tmp/nse-swarm-r10`, branch `swarm/r10-forensics-25368` off `origin/main` 184fc857
- Defect: BUG-305 — audit G1 remainder (runtime broker-offset override only partially adopted)

## Why this item

Role-10 lens = financial/time accounting correctness. The round-robin landed on role 10
(Accounting/Forensics), and the highest-value OPEN role-relevant item on the ledger is
**audit G1** (`docs/agent_handoffs/2026-09-07_NSE_audit_findings_and_status.txt:281`):
the broker server-UTC offset hardcode. A runtime override accessor already existed, but
was only partially adopted — the exact "configurable seam with a bypass consumer" shape
(BUG-303 class), and it sits on the live entry path (money) plus the history-read path
(accounting day boundary).

## Evidence (file:line at origin/main 184fc857)

Accessor + already-wired consumers (correct today):
- `src/nexus_scalp/adapters/mt5/providers.py:46` — `BROKER_SERVER_UTC_OFFSET_MINUTES = 180`
- `providers.py:57` — `get_broker_server_utc_offset_minutes()` (env `NSE_BROKER_SERVER_UTC_OFFSET_MINUTES`, validated 0..1440)
- `providers.py:149` — `broker_epoch_to_utc` uses the accessor
- `adapters/database/broker_history.py:308-312` — `_epoch_utc` uses the accessor
- `incidents/timebase.py:239-242` — `timebase_event_chain` uses the accessor

Bypass consumers (raw constant — the defect):
- `adapters/mt5/mt5_adapter.py:610,617` — `get_history_orders` default window
- `adapters/mt5/mt5_adapter.py:656,663` — `get_history_deals` default window
- `adapters/mt5/mt5_adapter.py:794,795` — `get_tick_history` input-window shift (BUG-188 shape)
- `execution/lifecycle/dispatch.py:326` — LIVE maintenance-window entry guard (money path)

Impact if an operator sets the override (e.g. 120 for EET winter, the G1 scenario):
epoch OUTPUT converted at 120min while INPUT request windows are built at 180min =>
net 60-minute skew on every history/tick read, and the entry guard gating on a clock that
differs from the rest of the timebase.

## Fix

Route all seven executable raw reads through `get_broker_server_utc_offset_minutes()`;
swap the adapter import; the dispatch guard caches the accessor result once per evaluation
and logs it. No contract/model/threshold change; `RemoteMT5GatewayAdapter` untouched
(never modified).

## Verification

- `tests/unit/test_bug305_broker_offset_env_consumer_parity.py` (NEW, 7 tests,
  `critical_suite.txt`-pinned): behaviour pins on a mocked native mt5 surface with
  `HAS_NATIVE_MT5=True` — tick input-window shift, history deals/orders default upper
  bound = `now + configured offset`, env-unset default byte-identical; class-wide AST
  guard (`ast.Name` loads) against any executable bare-constant read returning in either
  module; dispatch accessor source pin.
- RED-BEFORE EXECUTED on a pristine 184fc857 tree (stash both src files, immediate pop):
  4/7 red — both AST guards, the tick-window pin, and the dispatch pin.
- GREEN 7/7 post-fix.
- Neighbor census RC=0: **70** (maintenance_dispatch_guard, bug264_maintenance_pin_collision,
  broker_offset_config, maintenance_swap_policy, mt5_accounting_from_history) and **187**
  (accounting timezone/pnl/dedup, bug256 dispatch-halt, bug261 tick-domain,
  mt5_providers_phase14, economic_assumptions, incidents).
- Linters: `ruff check` clean; `ruff format --check` clean (1 reformat applied);
  `mypy` Success on both src files.
- Manifest: 199 entries, 0 missing.
- PRE-EXISTING host red disclosed and baseline-proven identical at 184fc857:
  `tests/unit/test_mt5_tick_boundary_bug188.py` 4/4 (its own harness never sets
  `HAS_NATIVE_MT5`, so the mocked call is never reached — not caused by this change).

## Files

- `src/nexus_scalp/adapters/mt5/mt5_adapter.py`
- `src/nexus_scalp/execution/lifecycle/dispatch.py`
- `tests/unit/test_bug305_broker_offset_env_consumer_parity.py` (new)
- `tests/critical_suite.txt`
- `agents/bugs.md`
- `docs/agent_handoffs/swarm_log.md`

## Next-run plan (role 11 CI/GitOps)

- Land this commit via PR (branch protection); confirm post-merge gates at the squash sha.
- Role-11 candidates: `NIGHTLY-E2E-CURLF` (operator, workflow scope), `SIGNED-RELEASE-ID`
  (operator), or triage of any red post-merge check-run; otherwise G3 (naive
  `datetime.now()` hold-time) is the next P1 time-truth item and is in role reach (11 can
  hand it to 6/2).
