# NSE-Swarm Run — 2026-09-10 08:20 UTC — Role 7 (QA/Tests)

**Scope:** Repo /tmp/NexusTradingForexBot had been deleted from /tmp (host cleanup) — re-cloned from origin/main at HEAD `82b228ff`, state restored from mirror. In-flight item AGENT5-REPLAY01-RED resumed per protocol.

## Defects fixed this cycle

### D-A: REPLAY-01 — live post-gap window never rebuilt (P1, FIXED-VERIFIED)
- Test: `tests/unit/test_agent5_replay_gap_parity.py::test_replay01_post_gap_window_rebuilds_like_dataset` (RED at 2fc1d84c and f3f53f69; live_first=None vs dataset_first=47).
- Root cause: `src/nexus_scalp/application/live_sequence.py` — after a >max_gap_us bar gap, `gap_invalid=True` was STICKY inside `maybe_build_sequence_tensor`: the boundary bar returned None and `gap_invalid` was cleared only by `LiveSequenceService.reset()`, which has ZERO production callers (grep: only test files + engine facade wrapper). Buffer could never refill → every window after one gap returned None forever → live serving starved after the first data gap, contradicting SequenceBuilder dataset parity.
- Fix: `gap_invalid` is now a one-window quarantine; the first fresh bar (not an intra-bar duplicate) re-arms the window and the buffer refills bar-by-bar, matching `SequenceBuilder` semantics (first valid post-gap window spans exactly the L bars after the hole). Sticky pre-append gate removed.
- Tests: REPLAY-01/02/03 green (101-test slice green); characterization `test_bar_ts_gap_detection` updated to pin the re-arm semantics.

### D-B: BUG-140 recovery-sweep suite broken by idempotency index (P1, FIXED-VERIFIED)
- Test: `tests/unit/test_outcome_recovery_sweep_bug140.py::test_expired_and_rejected_states` — `sqlite3.IntegrityError: UNIQUE constraint failed: audit_orders.execution_id` (CI-1067 at 82b228ff, only pytest failure in 1390).
- Root cause: commit c2662e31 (Agent-17) added partial UNIQUE index `idx_orders_execution_idempotency` on `audit_orders.execution_id`; the BUG-140 test helper `seed_dispatch` hardcoded `execution_id='EXEC-1'` for every row → second dispatch violates the index. Test-side defect (production INSERTs use real per-event identities + ON CONFLICT DO NOTHING).
- Fix: `seed_dispatch` now writes unique `EXEC-<request_id>` per row.

### D-C: accounting_api 401 failures (P1, FIXED-VERIFIED)
- CI nightly slow suite (runs 34443267998/34317134129): all `TestAccountingApi`/`TestWorkerWithEngine` tests got 401 on `/api/account/*`.
- Root cause: WEB-AUTH-P0 token auth (7c14451a, Sep 7) wraps `create_app`; `test_accounting_api.py` (last touched Sep 3) never opted out → 401. Nightly run1 (f2a5b8c5, Sep 7 20:51 UTC) predates the auth commit — that's why it only appeared from run2 on.
- Fix: `wired_app` fixture (and the one standalone TestClient test) set the documented PAPER-only `NSE_WEB_AUTH_DISABLE=1`. 15/15 green locally.

### D-D: e2e_05 doctor repair env-dependence (P1, FIXED-VERIFIED)
- CI: `test_e2e_05_doctor_fix_repairs_then_reverifies_to_ready` failed on every nightly run (assert 1 == 0).
- Root cause: doctor DATA check (555e6df2, Sep 7) fails on any machine without `data/raw/XAUUSD_M1.parquet`; `doctor --fix` cannot repair it → re-verify verdict DEGRADED → exit RUNTIME. The test ran in the repo cwd and asserted EXIT_OK unconditionally. (Locally it passed only because HOME had config; verified RED with empty-HOME probe, then GREEN.)
- Fix: test now chdirs to a fresh sandbox, seeds a deterministic 100-row canonical bars parquet + copies `configs/base.yaml` (RepairEngine template lookup is workspace-relative; without it config repair is SKIPPED), then asserts EXIT_OK and zero remaining FAILs. Verified green both with normal env and CI-like empty-HOME env.

### D-E (triaged, NOT fixed by swarm): task4 liq-audit FileNotFound
- `test_70d_model_26/27_liquidity_*_audit_smoke` fail on CI only: commit a77f8315 (TASK-AUDREV-HOUSEKEEPING) deleted scratch/ while `test_70d_model_validation_task4.py:749/772` still loads `scratch/archive/historic-20260823/liq60d_*.py` by path. Local runs pass because the files stay on disk (untracked). File exists at 7cbe6566 in git history. Owner: TASK-AUDREV-HOUSEKEEPING / Agent HK. Recommendation: restore the two audit modules into `tests/helpers/` or inline the probes.

## Verification
- Local slice: 101 passed / 0 failed (`tests/unit/test_cli_end_to_end.py tests/unit/test_outcome_recovery_sweep_bug140.py tests/unit/test_agent5_replay_gap_parity.py tests/unit/test_live_sequence_characterization.py tests/unit/test_temporal_sequence_contract.py`).
- ruff check + ruff format --check clean on all 5 touched files.
- Remaining nightly reds not owned by this swarm: test_41 (policy threshold 0.35 vs stale 0.20 pin — owner d57d003d should update `test_experience_intelligence.py:1337`), bug118 log-count tests, task4 scratch probes (D-E), lifecycle-chaos lane.

## Constraint compliance
- No changes to RemoteMT5GatewayAdapter/client contract, .github/workflows, frozen domain models, tick pipeline. Foreign WIP untouched. No cron changes.
