# NSE Swarm run 11 — CI/GitOps — 2026-09-12 19:55Z (renumbered BUG-263→BUG-264: rollback lane consumed 263 upstream)

## Defect: BUG-264 — maintenance-window test-pin collision (inclusive-edge off-by-one-minute)

**Status: FIXED-VERIFIED + LANDED** — PR #161 squash-merged as 955f372e (all required checks green incl. Py Tests windows+macOS); direct-main push rejected (branch protection), landed via PR per contract.

### Post-merge follow-through (previous run's plan, item 1)
- Main gates at 88a5fa15 + cb90e174: CI, Security, OSV, DepLock, Migration-safety,
  QA-Deep, Docs, JS all **success**. The `Tests (OS Matrix)` run (34705056262)
  at 88a5fa15 was **RED on windows-latest only** — investigated below (that is
  BUG-264, owned, fixed). cb90e174 (docs-only swarm-log commit) CI success.

### Root cause (file:line evidence at cb90e174)
- The buffered window is server **22:30..01:30 INCLUSIVE**
  (`src/nexus_scalp/research/economics.py:142-148` constants;
  `in_maintenance_window` inclusive compare at :253-256) == **UTC 19:30..22:30**
  at the canonical +180min offset (`src/nexus_scalp/adapters/mt5/providers.py:46`);
  guard consumption `src/nexus_scalp/execution/lifecycle/dispatch.py:324-348`.
- The pre-fix pin shape (`test_capital_protection_a15.py:295-299` at cb90e174):
  `now+180min`, on collision `+180min` **once**. Window is 181 minutes wide, so
  a 180-minute step lands on the inclusive edge: `now` in [16:30:00,16:30:59]
  UTC -> probe1 19:30:xx (in-window) -> retry 22:30:xx (in-window) -> first
  dispatch blocked -> `assert ... is True` RED.
- **CI proof**: run 1085 / job 103583502902 (windows-latest, 88a5fa15):
  `[ENTRY_BLOCKED] layer=MAINTENANCE_WINDOW ... decision_ts=2026-09-12T22:30:01.944 server_offset_min=180`
  then `FAILED ...::test_primary_path_duplicate_request_id_blocked - AssertionError: assert False is True`.
- Same class, wider band: wall-clock `datetime.now(UTC)` proposals reaching
  `dispatch_order` in test_bug258 (`_reversal_decision` pin, `_outside_maintenance`)
  and agent11/agent12 breaker/idempotency tests are red for the whole UTC
  19:30..22:30 band (baseline probe at pristine cb90e174 **inside** the band:
  6/6 RED pre-fix on exactly those tests).

### Fix (test-only, zero src/ change)
- NEW `tests/unit/maintenance_time_helpers.py::outside_maintenance_utc(now)` —
  advances in 180-min steps until PROVABLY outside (orbit period 8, window
  holds <=2 consecutive orbit points -> termination guaranteed); returns `now`
  untouched when already clear; AssertionError (fail-loud) if geometry changes.
- Pinned through the helper: test_capital_protection_a15 (DUP-1 test),
  test_bug258 (_reversal_decision + _outside_maintenance alias), agent11
  (breaker + reset tests), agent12 (exposure/idempotency/safe-mode/reset).
- NEW `tests/unit/test_bug264_maintenance_pin_collision.py` (6 tests, added to
  `tests/critical_suite.txt`): legacy collision re-demonstrated at the exact
  CI instant; second-resolution day sweep proving the red band is EXACTLY
  16:30:00..16:30:59 and the helper's band is empty (86400 samples); minute
  sweep; +-90s edge sweeps; untouched-when-outside; fail-loud geometry probe.
- Fix-in-passing (pre-existing, baseline-proven red at cb90e174):
  agent11 `test_web_layer_never_calls_adapter_directly` WEB-AUTH-P0 401 ->
  documented `NSE_WEB_AUTH_DISABLE=1` fixture opt-out (test_rule_matrix precedent).

### Verification (executed, not claimed)
- `pytest test_bug264 + test_capital_protection_a15 + test_bug258 + test_bug256 +
  test_maintenance_dispatch_guard`: **61 passed RC=0** (run at 19:50 UTC, inside
  the legacy collision hour class).
- `pytest test_agent11 + test_agent12`: **30 passed RC=0** (same in-window hour).
- `test_critical_suite.py` meta: 3 passed; every manifest path exists on disk.
- ruff check + format: clean (touched files + repo-wide tests/unit 415 files).

### Nightly-red triage (NOT owned by this wave)
- `Nightly Client E2E (Docker)` @ 80a34fc2: `NSE_PG_PASSWORD is missing a value:
  set NSE_PG_PASSWORD in .env` — workflow/repo-secret config gap (compose
  interpolation), not a code defect; needs operator/GH-variable action
  (same family as UPDATE-SIG-OPERATOR). Left OPEN.
- `Nightly Deep QA / chaos` @ 80a34fc2: PowerShell job step crashed in
  `Out-File` before pytest ran: `Could not find a part of the path
  'D:\chaos\pytest.txt'` — `$CI_RESULTS_DIR` empty/unexpanded in that pwsh step
  (`.github/workflows/nightly-qa.yml:113-118`). Workflow file — OUT OF SWARM
  EDIT SCOPE (`.github/workflows/*` untouchable from this job); operator push
  required. Recorded, not suppressed.

### Next-run plan
- Role 12 (Reviewer/Gatekeeper) or top-P0: K2/K3 paper-realism wave
  (`2026-09-07_NSE_audit_findings_and_status.txt`:374-380 — paper seed baselines
  hardcoded; no paper-vs-demo bias job).
- UPDATE-SIG-OPERATOR still parked on operator (NSE_UPDATE_SIGNING_KEY secret).
- HOLD_DURATION_CLOCK still UNWIRED (taskboard follow-up).
- Operator TODO: nightly-qa pwsh CI_RESULTS_DIR fix + NSE_PG_PASSWORD secret.
- Follow-up candidate (same class as BUG-262): outcome_repair not_before=
  decision_timestamp assumes decision~entry; tighten with fill-deal time.
