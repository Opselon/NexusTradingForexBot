# NSE-Swarm Run Report — role 7 (QA/Tests) — 2026-09-14T13:12Z

## Item worked
**BUG-276 (P1) — baseline-skeleton CONSTRAINT shadow: gate-first fresh DBs dead-letter EVERY `ON CONFLICT` producer forever** — FIXED-VERIFIED (commit pending via PR).

Resume note: the previous run (role 6) parked only the closeout paperwork, which landed while this run was starting — PR #194 (squash 53ef0191) + PR #195 (docs) are on main at 79401911 (verified 0/0 with origin at run start, clean tree). State file role_index=7 → new pick this cycle.

## Selection rationale
The state ledger's role-7 candidates were: NIGHTLY-E2E-PGPASSWORD re-triage (top of run), MIRROR-TEST-CLASS (P3), HOLD_DURATION_CLOCK sweep. The nightly E2E (08:03Z run 34820773492 at 79401911, first run on the dcc57826 seed fix) is STILL RED — triaged it first and it produced a far bigger fish than any queued item: the log shows a second, distinct, live defect at ~19 rows/second.

## Root cause (file:line evidence, all verified live)
- The PERF-DEADLETTER wave (2026-09-10) healed migration baseline skeletons to the app's COLUMN contract (`database/app_columns.py` APP_REQUIRED_COLUMNS) — but the skeleton also replaces the app's UNIQUE constraints with a bare `id INTEGER PRIMARY KEY`, and nothing can restore a missing unique index afterwards (app bootstrap CREATE TABLE IF NOT EXISTS no-ops; ADD COLUMN can't build indexes).
- SQLite refuses `INSERT … ON CONFLICT(<cols>)` outright when no UNIQUE/PK index covers exactly `<cols>` → every producer upsert fails → the batch-recovery loop (audit_repository.py:~2085) dead-letters EVERY row of that table forever.
- Reproduced at HEAD 79401911 with the exact production sequence (gate `nexus db migrate` on a fresh DB, then AuditRepository bootstrap): **15 ON CONFLICT targets FAIL**, only audit_signals/audit_orders/audit_executions survive (they self-heal their indexes inside the app bootstrap — the precedent this fix follows).
- Live proof in the failing nightly container (evidence artifact core.log): `Audit batch insert failed … dead_lettered=19 … ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint` from the first tick — the audit_guard_telemetry path (`_log_guard_telemetry`, audit_repository.py:2433, fed by TICK_DUPLICATE_SUPPRESSED at tick cadence).
- Full affected set: audit_guard_telemetry, audit_experiences, audit_experience_outcomes, experience_model_registry, strategy_registry, research_runs, research_worker_state, strategy_intelligence_registry, position_lifecycle_events, trade_autopsies, behavior_analysis, behavior_detections, anomaly_events, strategy_evolution_candidates, intelligence_worker_state — i.e. the entire learning-loop evidence chain (ledger → registry → research) silently destroys rows on every fresh install.

## Fix (3 files)
1. `src/nexus_scalp/database/app_columns.py`: new `APP_UNIQUE_TARGETS` contract (table → ON CONFLICT column tuples, stdlib-only, copied from the producers' SQL).
2. `src/nexus_scalp/database/engine.py` `_create_baseline_tables`: builds the unique indexes at baseline (fresh tables are empty in the same transaction); on a partially-populated DB it pre-checks existing rows — genuine duplicates are logged LOUD (`BASELINE_UNIQUE_SKIPPED`) and skipped rather than bricking the migration.
3. `src/nexus_scalp/adapters/database/audit_repository.py`: `_ensure_unique_constraint_heal` at the end of `_create_sqlite_tables` — repairs EXISTING poisoned field DBs at every boot. PRAGMA-gated zero-churn on healthy DBs (app-first boot adds ZERO indexes — pinned); duplicates → loud error + skip, boot never crashes. Helper `_unique_target_resolves` mirrors SQLite's conflict-target rule (UNIQUE index exact-column match incl. implicit auto-indexes, or exact PK).

## Regression battery
NEW `tests/unit/test_bug276_unique_constraint_shadow.py` — **27 tests, added to tests/critical_suite.txt**: 15 gate-first target-resolution pins; 6 real-producer-SQL replay pins (double-execute asserts dedup/aggregation + zero dead letters); the exact `log_signal(TICK_DUPLICATE_SUPPRESSED)` worker-path pin (count→2, dl=0); poisoned-DB boot-heal (pre-heal raises the E2E's exact error string); genuine-duplicate survival; healthy-DB zero-churn + boot stability; CLASS GUARD regex-scanning every `INSERT … ON CONFLICT` in src/ against a gate+bootstrap DB (scanner self-checks ≥18 targets so regex drift can't silently pass).

## Verification (all executed this run)
- battery 27 passed RC=0; PERF-DEADLETTER + database-platform lanes 33 passed RC=0; BUG-254-idempotency + dead-letter split/retention + critical_suite meta 29 passed RC=0.
- `-k "ledger or telemetry or research_worker or strategy_registry or provenance or database"` full slice: only failure = test_alt_ui_runtime_contract — baseline-proven pre-existing (fails identically at 79401911 in a clean worktree; UI-source pins, foreign to this change).
- `scripts/ci/check_migration_safety.py` RC=0 (audit/news/candle domains reach expected version on disposable DBs).
- Field-shape probes: poisoned-DB boot-heal 14 indexes + loud skip on the duplicated one, no crash; partially-populated-with-dups migrate = SUCCEEDED + skip + 14 healed.
- ruff check + format repo-wide clean; mypy Success on the 3 touched src files.

## Secondary finding — NIGHTLY-E2E-CURLF (OPEN-OPERATOR, workflow file out of this job's write scope)
The same nightly run's hard failure is "engine did not become ready within 300s" while `ps.txt` shows `Up (healthy)`. The readiness loop (.github/workflows/nightly-e2e.yml:72-77) polls with `curl -fsS`: `-f` discards the body on 503, and `/health` answers DEGRADED *with* 503 — docker/healthcheck.sh parses the 503 body precisely for this reason. A DEGRADED-but-operational engine can therefore NEVER pass the workflow probe; the container itself was healthy and serving. Suggested one-line fix for an operator/CI-role run: use `curl -sS` and parse the body (mirror healthcheck.sh), or probe `/health/ready`. Logged in agents/bugs.md BUG-276 NOTE.

## Landed/next
- Files: engine.py, app_columns.py, audit_repository.py, new test, tests/critical_suite.txt, agents/bugs.md (BUG-276 entry). Commit `NSE-Swarm role7: BUG-276 …`; push requires a PR branch (branch protection) — squash-merge when green.
- Next: role 8 Security. Carried: NIGHTLY-E2E-CURLF (fix is a workflow edit — needs operator or workflow-scope lane), MIRROR-TEST-CLASS P3, K3 paper-vs-demo (BLOCKED-HOST-DATA, 13th check pending), UPDATE-SIG-OPERATOR (secret still missing), first-field-DB observation for the boot heal on the next nightly E2E (guard-telemetry dead-letter flood must be GONE post-merge — that's the live acceptance test for BUG-276).
