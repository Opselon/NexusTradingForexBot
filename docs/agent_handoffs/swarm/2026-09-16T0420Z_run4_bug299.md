# NSE-Swarm Run Report — role 4 (Research/Backtest) — 2026-09-16T04:20Z

## Defect: BUG-299 — P0-3 friction-cap saturation: the canonical-costs research bridge truncated friction to 25% of calibrated cost and the robustness GATE8 was an exact no-op that PASSed everything

**Status: FIXED-VERIFIED (local, RED-BEFORE executed) → PR this run.**

### Root cause (file:line at 2ae009c0)

- `src/nexus_scalp/configuration/execution_costs.py:239-246` — `to_research_assumptions()` converts the canonical evidence (`configs/execution_assumptions.json`: spread.mean 0.147 USD + slippage p95 0.05 USD, price_tick 0.01) into `spread_ticks=15, slippage_ticks=5` but never set `max_slippage_ticks`, inheriting the model default **5.0** (`src/nexus_scalp/research/models.py:174`).
- `src/nexus_scalp/research/metrics.py:143-144` — effective friction = `min(spread+slippage, max_slippage_ticks)` = **5 ticks instead of the calibrated 20**. Every default-costs engine (`ResearchPipeline` → Backtest/WF/OOS/Robustness, pipeline.py:150-161; wired by `live_engine.py:1101`) silently paid 25% of money-path cost.
- Because the +1/+2-tick stress scenarios (`robustness.py:31-39`) and `_friction_sensitivity` (`metrics.py:289`) re-apply the same clamp, baseline and all stressed bundles pinned to the same 5.0: **all six stress expectancies byte-identical, `max_degradation` a constant 0.0, GATE8 PASSed every candidate**, and scoring's robustness term = 1.0 (`scoring.py:196`). This is exactly P0-3 AC-3 "silent no-op forbidden" (`docs/agent_handoffs/2026-09-09_p0-3_research_gate_integrity_handoff.md`), via a path the handoff didn't see (it flagged latency only). The economics lane had already escaped the same trap (`economics.py:77-79`, cap=40) — the canonical bridge never got it.

### Executable evidence (probes, not claims)

- Probe (50 trades, risk=1.0, r=+0.10, canonical costs): expectancy **0.0** pre-fix (cap 5) vs **-0.30** at cap 40 — 15 ticks of hidden edge per trade; a sub-0.1R "profitable" strategy measured exactly breakeven.
- RED-BEFORE on clean worktree `/tmp/nexus-bug298-red` @ 2ae009c0: battery 8 failed / 1 passed — the cap-pinned bundle returned `status=PASS, max_degradation=0.0`, six identical stress values (the defect verbatim).

### Fix (3 src files, no threshold weakened)

1. Bridge derives the cap: `max(5.0, spread+slip+STRESS_HEADROOM_TICKS=4)` — covers evidence + documented stress sweep; low-friction bundles keep the legacy 5.0 guard.
2. `RobustnessEngine` computes effective friction per scenario via a shared-semantics helper; unchanged-friction scenarios land in new honest census field `RobustnessResult.not_simulable_scenarios`; **if NO scenario moves effective friction (cap-pinned or empty scenario set) → status=FAIL `ROBUSTNESS_NOT_SIMULABLE` + warning log**. A PASS from a dead gate is impossible by construction. Latency scenarios (genuinely unpriced by the deterministic backtest) are recorded, never presented as measured results.

### Regression net

`tests/unit/test_bug298_friction_cap_saturation.py` (9, critical_suite-pinned): bridge cap/equality/low-friction-floor trio; cap-pinned fails-closed; empty-set fails-closed; healthy-bundle monotonic degradation + exact latency census; default canonical engine measures something (pre-fix zero); source pins (bridge sets cap; metrics↔robustness formula parity — MIRROR-TEST-CLASS pattern).

### Test results

- Battery 9/9 GREEN at fix; 8/9 RED at 2ae009c0 (RED-BEFORE, executed).
- Neighbors RC=0 (~350 tests): phase09b 90, task4_validation, execution_costs_canonical, zero_friction_guard_e1, economics/parity/phase10, lifecycle_e2e_v3, hardening, walkforward-agent5, time-machine, history-surface, archive, regime-report, provenance, registry-null, selection_bias 16.
- One honest fixture consequence: `test_research_selection_bias._sub_economic_dataset` baked the 0.025R truncation into its "adjusted ≈ +0.01R" intent; with the true 0.1R friction the same raw pair lands negative → re-calibrated raw values (0.71/-0.49) to keep testing the same (0, 0.02R) band. Intent unchanged, comment documents it.
- Pre-existing host reds disclosed, **identical at clean 2ae009c0 same-session**: bug233 ×2 + research_observability_phase21 ×7 (`research_events_archive`/`research_evidence_archive` table env shape) → monitor row REDSH-RESEARCH-FAMILY-ENVIRO.
- ruff check + format repo-wide clean (2057 files); mypy Success (3 src files); manifest 167 paths 0-missing.

### Implication for the program

Every historical OOS/expectancy number computed with default costs understates friction by 15 ticks. Champion retrain economics (P0-5 gate) must be re-measured under true costs; expect stricter gates (fewer VALIDATED candidates) — the correct direction per P0-3. Follow-ups opened: OOS-floor rationale truthfulness pass, degradation-cap 1.0 tightening decision, as_of discovery restriction (P0-3 item 1).

### Landed

Branch `swarm/bug298-friction-cap-saturation` (off 2ae009c0) → PR → squash merge (sha recorded in swarm_log line).
