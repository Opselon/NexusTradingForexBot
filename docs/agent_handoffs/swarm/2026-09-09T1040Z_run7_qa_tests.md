# NSE Swarm Run — 2026-09-09 ~10:40 UTC — Role 7 (QA/Tests), cont.

## Item: SMOKE-COUNT-CI (in-flight, PARKED -> root-caused -> FIX LANDED locally)

### What this run did
1. Synced origin/main (fast-forward, no divergence). HEAD 890951f9.
2. Confirmed the failing artifact class via CI run 34301659830 (sha 0630971f,
   BEFORE the 11 local commits):
   - `FAILED tests/unit/test_smoke_self.py::TestDurationGuard::test_full_smoke_under_budget
     - AssertionError: full smoke produced only 37 checks — want ≥40` (junit.xml).
3. Downgraded the presumed "xdist 5-way collapse race" hypothesis:
   - New dominant root cause found: CI L5 boot died BEFORE the L3 layer because
     `build_gate_engine` provisioning (scripts/ci/runtime_gate.py:695-730) wrote
     model.pt + manifest.json NON-ATOMICALLY (direct torch.save + write_text),
     exposing torn readers (LEGACY_UNVERIFIED / HASH_MISMATCH) — plus the
     provisioned bundle has NO model.scaler.npz, so runtime_gate L4
     (`l4_model_contract`, runtime_gate.py:612-618) FAILs with
     "artifact width split: checkpoint=70 scaler=None (want 70/70)" the moment
     a provisioned (non-provenance) bundle persists on a runner.
   - Reproduced locally: with the repo-local provisioned bundle (no scaler),
     `runtime_gate.py --json` rc=4 BLOCKED (L4 FAIL, same message);
     test_runtime_gate.py::test_json_subprocess_output_is_pure_json fails
     (exit 4 not in (0,3)).
4. Fix (script, artifacts/swarm_agent/provision_gate_bundle.py):
   provisions a FULL 70D servable bundle the way the trainer does
   (model.pt + model.scaler.npz + model.meta.json + manifest.json with
   model_sha256/metadata_sha256/scaler_sha256 bindings, manifest written LAST).
5. Verified:
   - runtime_gate.py --json -> rc=0 CERTIFIED, L0..L9 all PASS.
   - pytest tests/unit/test_smoke_self.py -> 13 passed in 21.38s
     (includes test_full_smoke_under_budget count>=40).
   - artifact sha256 48efa5d9... model.pt / scaler 1060 bytes / meta+manifest ok.
6. Blocked: git push of the 11 local commits (7a5ab3fa..HEAD incl. CI mkdir fix
   + deplock uv pin) — remote rejects OAuth app without `workflow` scope
   (`.github/workflows/ci.yml` touched). User must push manually.

### Next run
- Validate check-count stability under -n auto (slim venv) once more.
- Consider landing the full-bundle provisioning INTO runtime_gate.py
  (l4 SKIP when scaler absent vs provision scaler too) — needs care: gate is
  certification surface; changes go through beforePush parity on CI.

## Environment note
- Local artifacts/ models dir seeded with swarm bundle (gitignored).
- stash@{0} "pre-mission-papersim-wip" predates this run — untouched (12 files).
