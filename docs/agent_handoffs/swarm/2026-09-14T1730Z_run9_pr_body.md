# NSE-Swarm role 9 (Observability): BUG-277 — doctor WARNING-actions filter dead (rendered label vs producer vocabulary)

**Defect:** `nexus doctor` closing panel: `warns = [e for e in entries if e.verdict == "WARN"]` (doctor.py:235 pre-fix) compared the RAW `HealthEntry.verdict` against the string only the RENDERER produces (`styling.py:_verdict_style` maps "WARNING" -> "[yellow]WARN[/yellow]"). Producer vocabulary is PASS|WARNING|FAIL (`release/health.py:99`). `warns` was therefore permanently empty.

**Impact (P2):** the 2026-09-02 "actionable closing summary" (USER ACTION count + `NEXT: <suggestion>`) never surfaced WARNING guidance. A DEGRADED install with a schema-behind audit.db (DATABASE WARNING "Run `nexus db migrate`" — the exact BUG-254/276 dead-letter pre-condition) was told `USER ACTION: 0` / `NEXT: nexus start (paper mode by default)`. FAIL path and JSON output unaffected.

**Fix:** filter on the producer vocabulary `("WARNING", "WARN")`; adjacent drift pin: banner `"system doctor · 24 checks"` literal -> `len(entries)` (run_all() is the SSOT).

**Regression:** NEW `tests/unit/test_bug277_doctor_warning_vocabulary.py` (8, added to critical_suite): 4 CliRunner behavior pins (`_health_entries` stubbed — RED-BEFORE 4/8 at baseline be1842d2 in a pristine worktree), producer-vocabulary contract, styling source-pin, class-wide regex guard (no `verdict ==/!= "WARN"` may return in src/; `status == "WARN"` users — smoke runner, release verify — own a different PASS|FAIL|WARN|SKIP contract, excluded by field name), md7 source-pin.

**Verification:** battery 8 passed RC=0 / 4/8 RED pre-fix; lanes RC=0: 64 (release_system + perf_health_probe_cache + bug275 + bug276), 5 (cli-e2e -k "doctor or health"); ruff check+format repo-wide clean (1993 files); mypy Success doctor.py; 3 runtime_truth host reds BASELINE-PROVEN pre-existing (data/raw absent, 14x).

**Role-9 wave:** dead-letter observability WIRED (debug_snapshot.py:970-978 + per-event WARNING + retention cap) — no gap. /health 503 body already carries per-category DEGRADED reasons — NIGHTLY-E2E-CURLF (workflow `curl -f`) stays the only blocker, operator-scope. NEW LEDGER `HEALTH-WIRING-UNDECIDED`: `check_worker_liveness` + `check_a2a_gateway` defined (health.py:518/:573) but absent from `run_all()` — deliberate leave, verdict-surface change.

**Acceptance pending:** BUG-276 live proof = 09-15 ~07:30Z nightly (first scheduled run at/after 1b66e014): zero ON-CONFLICT dead-letter lines.
