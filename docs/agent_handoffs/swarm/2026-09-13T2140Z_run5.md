# NSE Swarm Run — 2026-09-13T21:40Z — Role 5 (Data/Ingestion)

## Closeout verified first (from previous run's next_run_plan)

- Post-merge main gates at **3ad2b85f**: ALL green (15 check-runs: CI, Security/CodeQL,
  OSV, Trivy, DepLock, Migration, QA-Deep/deep-assurance, JS, Docs, CI-Integrity,
  Py Tests windows+macOS, Pages). No DepLock recurrence this hour. Verified via
  `gh api .../commits/3ad2b85f/check-runs` at run start.
- NIGHTLY-E2E-PGPASSWORD: latest nightly-e2e run (34745655070, 09-13 07:36Z, head
  c1db4ac4) still failed — but `git merge-base --is-ancestor dcc57826 c1db4ac4` proves
  the fix commit (dcc57826, PR #188) LANDED at 21:37 +0330 = 18:37Z, i.e. AFTER that
  run started. The failure log is the exact old NSE_PG_PASSWORD interpolation error.
  Status: fix unverified-by-execution (next nightly will be the first with the seed);
  ledger stays LIKELY-RESOLVED-BY-OTHER-LANE.

## Defect worked: BUG-257 remainder → ledger'd BUG-272 (champion-drift sentinel)

**Role lens (Data/Ingestion):** artifact integrity = data trust. The P0-2 trust anchor
(model_bundle_store._verify_champion_registry_binding, model_bundle_store.py:303-395)
compares serving model.pt sha16 vs the governed CHAMPION row fingerprint — at BOOT only.
BUG-271 made governed writes attributable; the open remainder (BUG-257 RISKS/OPEN (1),
agents/bugs.md:8449) is that an OUT-OF-PROCESS rewrite while the engine runs (the exact
09-11 04:13 bb1f0afe re-publication shape) is served silently for the whole uptime; the
refusal only fires at the next cold boot, after unbounded trading on unverified bytes.

**Fix (alert-only, never mutates/halts):**
- `src/nexus_scalp/model_lifecycle/champion_sentinel.py` (new, 155L):
  `evaluate_champion_drift` PURE verdict (MATCH/DRIFT/INERT; sha16 comparison byte-
  identical in semantics to the anchor → a DRIFT verdict ⟺ the anchor would refuse —
  pinned by an equivalence test through the real `ModelBundleStore` helper);
  `probe_champion_drift` I/O leg via the URI-aware `audit._connect_sqlite` single
  connect site (audit_repository.py:1953), reads newest CHAMPION row +
  `fingerprint_artifact` (provenance.py:38), serving path prefers the LOADED bundle
  artifact (hot-swap safe) over config. Import-light at module scope (no torch/polars;
  pinned) — maintenance-path gating lesson.
- Wired at `MaintenanceCycle.run_cycle` (maintenance.py:121-201), the single owner of
  time-throttled housekeeping, driven from live_engine.py:2116 (`_maintenance.run_cycle`,
  run_loop + duplicate-tick heartbeat) — a production runtime caller, off the tick path
  via `asyncio.to_thread`, fully failure-isolated:
  * None "never ran" throttle sentinel → first check ALWAYS due (the `_last=0.0` vs
    monotonic trap, pinned by test).
  * 900s interval; TWO consecutive DRIFT sightings before ONE alarm (absorbs the
    governed-writer window: the file write precedes the FIFO-queued supersession —
    a single sighting must not cry wolf); re-alarm every 4th sighting; CHAMPION_DRIFT_
    CLEARED warning on resolution; INERT resets silently.
  * Alarm = `logger.critical("[CHAMPION_SENTINEL] event=CHAMPION_DRIFT_CONFIRMED …")`
    + Telegram severity=CRITICAL event_type=CHAMPION_DRIFT_CONFIRMED (suppressed-safe).
  * Trust anchor byte-unchanged; INV-015 enforcement stays the boot anchor.

## Bonus (same tree, cheap, from the previous plan): LEARNFIX1-PROVENANCE-PREEXISTING-RED CLOSED

Triage asked: CI status of the 2 reds at main tip. Verdict: they are NOT in
tests/critical_suite.txt and not named in any workflow → never run in CI; the local
reds were REAL, not a host-env artifact. Root cause found by direct probe
(/tmp/probe_learnfix_shape.py): b1eb5b94 (BUG-244, 09-05 04:44) smoke-scoped the
emission gate AFTER the tests were written (541c43c6, 09-05 03:07) — a smoke=True 50D
drill now legitimately PUBLISHES (quarantine contract), so `pytest.raises(EMISSION_GATE_
ABORT)` can never fire for the smoke fixture. Fix: the two gate-pin tests run with
smoke=False (the path where the contract IS hard: probe shows
`EMISSION_GATE_ABORT: actual input dim 50 != canonical 70`, emission_gate.py:106).
No assertion relaxation — the strengthened posture is the point. 6/6 green.

## Evidence (all executed this run)

- Sentinel battery: tests/unit/test_bug257_champion_drift_sentinel.py **11 passed
  RC=0** (added to tests/critical_suite.txt in the same commit — manifest lesson).
- Lane green: learnfix1 + bug271 + sentinel = **30 passed RC=0**; neighbors:
  critical_suite meta + storage_lifecycle_wiring + calendar_maintenance_wiring +
  live_workers_characterization = **17 passed RC=0**; agent3_champion_registry_sync +
  persistence_boot_trust + champion_bundle_recovery + hot_swap_governance + bug269 =
  **23 passed RC=0**; p0_producer_regression 9 passed RC=0.
- RED-before shape: the sentinel stage did not exist at HEAD before this change
  (wiring pin + every stage-behavior test fail without the new code).
- Gates: `uvx ruff check .` all passed; `ruff format --check .` 1986 files clean;
  `mypy` Success on champion_sentinel.py + maintenance.py.
- K3 re-check: `ls data/raw` → ABSENT (11th consecutive check; operator export still
  the prereq).

## Files

src/nexus_scalp/model_lifecycle/champion_sentinel.py (new) ·
src/nexus_scalp/application/live/maintenance.py ·
tests/unit/test_bug257_champion_drift_sentinel.py (new, gated) ·
tests/critical_suite.txt · tests/unit/test_learnfix1_provenance_binding.py ·
agents/bugs.md (BUG-272 row) · docs/agent_handoffs/swarm/2026-09-13T2140Z_run5.md ·
docs/agent_handoffs/swarm_log.md (+ previous run's unpushed closeout line)

## Next-run plan (role 6 Runtime/Linux)

1. Closeout: verify post-merge gates at the landed sha (PR route; branch protection).
2. Candidates: HOLD_DURATION_CLOCK (unwired, ledger) · clock-domain residue sweep on
   remaining exit-path consumers · BUG-257 writer-attribution follow-up now that the
   sentinel exists (first real DRIFT sighting becomes evidence).
3. Operator TODOs unchanged: NSE_UPDATE_SIGNING_KEY (blocks 9.0.12 tag),
   nightly-qa CI_RESULTS_DIR pwsh path, data/raw export, docker image rebuild,
   BUG-271 one-time sqlite repair on already-orphaned installs.
