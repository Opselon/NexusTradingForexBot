# NSE Swarm run 3 — role 3 (ML/Training) — 2026-09-13T13:05Z start

## Item worked
**BUG-269** — Docker starter provisioner mints UNTRAINED weights the P0 serving gate
refuses -> fresh-volume deployment is a MODEL_LOAD_REJECTED restart loop.
Status: **FIXED-VERIFIED — LANDED-MERGED (PR #182 squash baf737ab; all post-merge main gates green incl. win+mac OS matrix)**.

## Causal chain (all file:line from eaf30234, re-grepped this run)
1. `docker/entrypoint.sh:124` runs `docker/provision_model.py` on every container start;
   on an empty artifacts volume it mints `ScalpNet(70,3)` at ambient random init
   (old lines 44-46: construct -> eval -> `torch.save(net.state_dict(), model)`) and
   writes a manifest digest computed from those same untrained bytes.
2. `application/live/model_bundle_store.py::_load_or_create_bundle` (100-135, added
   60b785d3 #154) verifies integrity (VERIFIED — the digest is self-referential,
   weights==sidecar only), then `detect_untrained_fresh_init` (no flag: ambient draw
   is not byte-equal to seed-42) and `check_model_behavioral_health` -> **FAILS**
   (`DEGENERATE:logit_std 0.073 < 0.15`, sensitivity 0.008) -> ArtifactIntegrityError
   -> launcher fatal -> restart -> identical failure. Provisioner's stated purpose
   (entrypoint comment) was to prevent exactly this loop.
3. Root pattern: the serving-gate wave fixed the CI twin `scripts/ci/runtime_gate.py:705-807`
   (seed-999 TRAINED mint + fail-loud seam + BUG-154 ordering) and missed the docker twin.
   Two provisioners, one contract, drifted.

## RED-before evidence (executed, not asserted)
`git show HEAD:docker/provision_model.py` run with NSE_WORKSPACE=/tmp/prov_test_a:
  -> "provisioned starter bundle" ; fresh-init (False, DIVERGES_AT:input_projection.weight)
  -> behavioral (False, 'DEGENERATE:logit_std 0.073 < 0.15') ; manifest VERIFIED.
Stash probe (fix reverted, same HEAD): 5/7 new tests FAIL (mint not servable,
no repair path, no fail-loud seam, nondeterministic ambient draw, source contract).

## Fix
`docker/provision_model.py` rewritten to the runtime_gate parity contract:
- deterministic TRAINED mint: `torch.manual_seed(999)` BEFORE construction, fixed
  generator(1234) data, 30 AdamW steps (BUG-154 ordering preserved).
- skip branch is SERVABILITY-gated, not digest-gated: digest-valid-but-degenerate
  (this bug's residue on live volumes) is re-provisioned loudly; a servable bundle
  (operator-trained champion passes the probe) is skipped byte-identical (mtime pin
  in tests); ImportError during the probe keeps the existing file (never clobbers
  what it cannot judge).
- fail-loud at the seam: unservable mint raises with probe detail (PROVISIONING_ERROR),
  never a mysterious deep-boot rejection.

## Verification (this run, all RC=0)
- New suite: tests/unit/test_bug269_docker_starter_servable.py — 7 passed (post-fix).
- Fresh end-to-end probe of the NEW script: mints sha16 **79707b153b9cd1a4**,
  byte-equal to the runtime_gate proven recipe; VERIFIED + BEHAVIORAL_HEALTH_PASS;
  idempotent re-run "bundle verified and servable, skip"; old-mint volume repaired
  in place ("re-provisioning digest-valid but UNSERVABLE bundle").
- Neighbor lane: test_bug266_paper_data_wiring (26) + test_champion_writer_governance
  + test_model_load_integrity = 38 passed.
- test_critical_suite.py meta green with the new gated entry (file added same commit).
- ruff check + `ruff format --check` clean on both touched py files (uvx ruff 0.14).
- No src/ diff: the serving gate is byte-unchanged (fix feeds it acceptable inputs,
  never relaxes it).

## Files
docker/provision_model.py, tests/unit/test_bug269_docker_starter_servable.py (new),
tests/critical_suite.txt, agents/bugs.md (BUG-269), docs/agent_handoffs/swarm/, swarm_log.md.

## Operator follow-up
- Existing containers/volumes already carrying the old starter self-heal on next start
  (entrypoint re-runs the provisioner) — no manual repair needed.
- Docker image rebuild required for NEW volumes to ship the fixed script (host builds
  are slow; not run this cycle — code path proven by direct execution above).

## Next-run plan (role 4 Research/Backtest)
- Original role-3 plan item BUG-257 writer-hunt: partially advanced this run —
  candidates for the 09-11 04:13 re-land were runtime_gate provisioning (sha now
  proven = 79707b153b9cd1a4 != bb1f0afe, so a gate run on the live host was NOT the
  writer) and the docker starter (bb1f0afe predates 50df3a1e's first-land? unresolved);
  the sentinel idea (doctor checks serving-bundle sha vs CHAMPION registry fingerprint
  OUTSIDE boot) remains open.
- Role 4 lane: research/backtest friction-gap audit (walk-forward OOS realism),
  or BUG-257 sentinel as security-adjacent follow-up.
- K3 bias job still BLOCKED-HOST-DATA (data/raw re-verified ABSENT this run).
