# NSE-Swarm Run — 2026-09-11T00:10Z — Role 2 Backend/Domain

## Item
TASK-AUDREV-C3 follow-up: C3 gate (b) session-percentile spread gate was DEAD WIRING.
- Policy hook existed (SignalPolicy.session_spread_percentile_fn, commit 76eb23b9, policy.py:744-766)
- Read-only provider existed (broker_history.session_spread_percentile, broker_history.py:467)
- NO runtime constructor ever bound them: `grep -rn "session_spread_percentile_fn" src` showed zero callers.
- Result: SPREAD_SESSION_PCT_EXCEEDED could never fire in production; the audit's "cheapest remaining real-P&L win" was silently disabled.

## Fix (branch swarm/c3-spread-pct-wiring, PR #118)
- live_engine.py: `_session_spread_percentile_provider()` — fail-isolated wrapper over the engine's own audit DB via `AuditRepository._connect_sqlite` (URI-aware, 5s timeout, connection closed per call, no cached handle). Unexpected faults clamp to None (honest no-op); non-SQLite DBs return None.
- Bound at composition root ctor (after model_identity_fn wiring, live_engine.py:1386).
- live_engine.py `_sync_runtime_config`: hot-reload parity for algo.spread_session_gate_enabled + algo.spread_session_percentile (operator toggle previously needed restart).
- Contract: INV-001 safe (single bounded read-only SELECT on indexed audit_paper_executions, only when candidate is spread-positive), honest-unknown (thin sample -> None -> gate no-op, never 0.0 fail-open), reads the DURABLE copy not the adapter in-memory ledger. No policy semantics/threshold changes, no IMT5Port changes, no workflows touched.

## Evidence (run, not claimed)
- NEW tests/unit/test_c3_spread_session_wiring.py: 11/11 GREEN on slim venv, incl. END-TO-END: seed durable copy -> engine provider -> policy eval -> SPREAD_SESSION_PCT_GATE block.
- test_spread_gates_c3.py (21) + test_config_contract_agent18.py: GREEN.
- Engine consumers (launcher_paper_boundary, latency_regression, obs_trace_chain): GREEN.
- ruff check + format --check: clean. mypy live_engine.py: Success.

## Landing
- Commit ec3e9afc on swarm/c3-spread-pct-wiring; pushed; PR #118 opened (direct push to main is protection-blocked by design).
- CI at PR: integrity/dep-lock/OSV/Trivy/migration PASS; Py Tests + Code Quality pending at report time.

## State
- role_index stays 12->2 backend lens; in_flight cleared; next run: watch PR #118 checks, merge when green (per step 7), then next_run_plan taskboard items (TASK4-SCRATCH-PROBES restore, test_41 0.20 pin).
