# NSE-Swarm run 1 (role 1 — Architect) — 2026-09-13 02:30 UTC

## Defect: BUG-266 — paper REPLAY mode shipped but never wired (audit K2 unreachable)

**Base HEAD:** b179d323 (main; post-merge gates verified all-green at start of run:
CI/Security/OSV/DepLock/QA-Deep/JS all success).

### Root cause (file:line at b179d323)
`PaperMT5Adapter` supports an explicit `REPLAY` market-data mode since a5f38467
(P0 phase 4) with a complete `ReplayTickSource` (dataset cache + real M1 CSV,
fail-closed loading, historical timestamps, causal `history_bars`). But the wiring
was dead: every production construction passed no `replay_source`:

- src/nexus_scalp/cli/engine_boot.py:455 `PaperMT5Adapter(symbol=...)`
- NexusTradingForexBot.py:641 `PaperMT5Adapter(symbol=...)`
- src/nexus_scalp/application/live_engine.py:4206 (align_adapter_to_boot_mode)
- src/nexus_scalp/application/live/runtime_mode.py:102 (hot-swap)

`grep -rn "replay_source=" src NexusTradingForexBot.py` outside the adapter itself:
zero hits. Consequence: audit K2 ("wire paper to real recorded data for
training-grade paper") was unreachable in production at ANY config — every PAPER
experience row trained on the synthetic AR(1) walk around `_SEED_BASELINES`.
Textbook "working class, no production caller".

### Fix
1. NEW `src/nexus_scalp/adapters/paper/paper_data.py::build_paper_adapter` — the
   single PAPER construction decision point (SYNTHETIC default byte-identical;
   REPLAY attaches a real `ReplayTickSource`; fail-closed `raise` policy for
   interactive boots; loud-degrade `synthetic` policy with
   `requested_mode`/`degraded_reason` provenance stamps for boundary
   re-alignments; `allow_replay=False` for SHADOW).
2. NEW bootstrap config section `AppConfig.paper_data`
   (mode/dataset_id/allow_raw_fallback/raw_bars_path) — env + YAML round-trip
   (NSE_PAPER_DATA__MODE), documented in configs/base.yaml. Deliberately NOT in
   the runtime-config snapshot: substrate change crosses BUG-232 invalidation,
   so it is boot-time only.
3. All four PAPER sites routed through the factory (AST source contract pinned).
4. BUG-212 launcher source test EXTENDED (not weakened): a factory bind satisfies
   the PAPER-boundary contract.
5. Honesty fix in test_paper_replay_mode.py: the raw-bars test errored on every
   clean checkout (data/raw is gitignored operator export) — now skips loudly
   when absent, still asserts real chronology when present.

### Evidence
- RED-before: `tests/unit/test_bug266_paper_data_wiring.py` AST contract failed at
  b179d323 ("launcher must bind ... for PAPER boots"); zero production
  `replay_source=` constructions.
- AFTER: 26/26 new tests green; 88 green across neighbor lane (bug232 boundary,
  launcher boundary, paper parity/persistence/replay, config agent18, runtime
  hot-reload, cli e2e); `scripts/ci/runtime_gate.py --json` rc=0 CERTIFIED
  (default SYNTHETIC path unchanged, tripwire delegation intact); ruff check +
  format repo-wide clean (1958 files); mypy clean on touched src files.
- Files: src/nexus_scalp/adapters/paper/paper_data.py (new), configuration/config.py,
  cli/engine_boot.py, application/live_engine.py, application/live/runtime_mode.py,
  NexusTradingForexBot.py, configs/base.yaml, tests/unit/test_bug266_paper_data_wiring.py
  (new, critical_suite-gated), tests/unit/test_paper_replay_mode.py,
  tests/unit/test_launcher_paper_boundary_bug212.py, agents/bugs.md.

### K3 follow-up (next Architect cycle, unchanged plan)
The bias job (paper-vs-demo comparison feeding calibration) now has a working
substrate: REPLAY paper sessions carry `replay_provenance` (source/record
count/range) on every experience row path — K3 can key bias measurement on the
dataset fingerprint. Filed as next-run candidate, not started this run.
