# NSE-Swarm run 12 — Role 12 (Reviewer/Gatekeeper) — 2026-09-12T23:30Z

## Headline
Cleared all three open CI-determinism items left in the BUG-264 lineage and
verified main's required gates green at df275c16.

## Items worked

### 1. Dependency-drift required gate RED on main (60b785d3) — RESOLVED (superseded)
- Reproduced the true positive locally: `check_dependency_drift.py --regen`
  with uv 0.12.10 yields exactly the pyinstaller 6.22.2 -> 6.22.3 arm swap.
- Opened PR #169 with the regen; while polling, the recovery lane merged
  PR #168 (df275c16) which carries the identical regen **plus the root
  cause**: #164 bumped setup-uv v7.6.0 -> v10.0.1 while
  `dependency-lock.yml` kept the `uv-version:` input, which v10 dropped as
  an alias — the pin was silently ignored, so CI resolved under a floating
  uv. Drift gate is green on main at df275c16 (check-runs API verified).
  #169 closed as superseded; no swarm artifact landed. Lesson: gate-red
  deplock after a dependabot actions wave = check BOTH pin sides first
  (workflow input shape AND script floor) — documented class, new shape
  (v10 reversed the v7 input rename).

### 2. BUG-264-FOLLOWUP-1 (role 12 assignment) — FIXED, PR #170 branch
`tests/integration/test_experience_execution_boundary.py`:
- `_proposal()` used raw `datetime.now(UTC)` into `dispatch_order`; the
  MAINTENANCE_WINDOW guard (execution/lifecycle/dispatch.py:299-346) blocks
  directional entries UTC 19:30-22:30 -> the dispatch battery is
  time-of-day dependent in the Heavy-CI integration matrix.
  Fix: pin via `tests/unit/maintenance_time_helpers.outside_maintenance_utc`
  (BUG-264 helper, proven termination). RED-before proof: scratch probe
  forcing the guard showed unpinned 21:00Z -> `dispatch_order` False + 0
  positions; pinned -> True + 1 position.
- REST test 401: `create_app()` without a token since WEB-AUTH-P0.
  Reproduced live at df275c16 (`assert 401 == 200`, line 358).
  Fix: `monkeypatch.setenv NSE_WEB_AUTH_TOKEN` BEFORE create_app (env-wins,
  DPAPI store untouched) + Bearer header + `delenv NSE_WEB_AUTH_DISABLE` —
  the established test_mt5_accounting_api_contract.py pattern. Auth contract
  NOT bypassed; the test authenticates like a production consumer.

### 3. BUG-185V140 (OPEN-ROUTED — resolved in passing) — FIXED, PR #170 branch
`tests/integration/test_end_to_end_closed_loop_bug140.py` STAGE 2 asserted
`contract_version == "p0e-bug140-1"` but the SSOT moved to `"p0e-bug185-1"`
in 5073b865 (src/nexus_scalp/research/dataset.py:150); the unit twin
(tests/unit/test_lifecycle_bug140.py:574) was updated in that commit, the
integration mirror was missed -> deterministic red on every HEAD since.
Fix pins the SHIPPED value (no relaxation) and moves STAGE-11's proposal
through the same outside-window pin (raw now, same collision class).

## Verification (all executed, slim venv, PYTHONPATH=src:.)
- `pytest tests/integration/test_experience_execution_boundary.py
  tests/integration/test_end_to_end_closed_loop_bug140.py` -> 6 passed RC=0
  (pre-fix: 1 FAILED 401 + closed-loop STAGE-2 assert failure reproduced).
- `pytest tests/unit/test_bug264_maintenance_pin_collision.py` -> 6 passed.
- ruff check + ruff format --check clean on both touched tests.
- Import-path audit: 11 test files make wall-clock proposals; only the two
  fixed files + bug258/a15/agent11/agent12 (BUG-264 wave) reach
  `dispatch_order`; test_launcher/audit_db_growth are source-inspection or
  ledger-only. Queue is now clean of this class.

## Ledger
- agents/bugs.md: BUG-265 entry (test-only, three-way fix, invariants).
- PR: swarm/bug264-followup -> main (test-only + agents/bugs.md).

## Next-run plan
1. Verify PR #170 checks green + merged; sync main.
2. Role 1 (Architect): top backlog = K2/K3 paper-realism wave (2026-09-07
   audit:374-380) — seed baselines hardcoded + paper-vs-demo bias job
   missing. Or sweep the integration matrix for any remaining
   time/env-dependent lanes now that auth/window classes are pinned.
3. Operator TODOs unchanged: NSE_UPDATE_SIGNING_KEY secret,
   NSE_PG_PASSWORD secret, nightly-qa.yml CI_RESULTS_DIR pwsh path.
