# NSE-Swarm Run — 2026-09-09 ~19:00 CEST — Role 8 (Security)

## Run metadata
- run id: 2026-09-09T1900Z_run8_security
- role: 8/12 Security (rotated from 7 QA/Tests)
- HEAD at start: 5f804488 (origin/main, all 5 main gates GREEN — CI/DepLock/OSV/QA-Deep/Security run 34379389082)
- previous run: 12:22Z (QA/Tests) — SMOKE-COUNT-CI closed via PR #114; no parked in-flight item

## Defect worked: UPDATE-SIG-GAP (new P0-class discovery, verified live)

**Claim in the audit ledger:** B5 "ed25519/minisign update signing STILL ABSENT".
**Reality at HEAD:** the client-side trust root LANDED (commit 37072a86, 2026-09-07):
- src/nexus_scalp/release/signing/ (canonical manifest, Ed25519 sign/verify, embedded trust root)
- src/nexus_scalp/release/update_engine/discovery.py:671-694 — plan stage verifies the
  signed manifest against the resolved digest, SECURITY_BLOCKED on any failure (fail-closed)
- tests/unit/test_signed_update_manifest.py (SIG-01..16) + phase17 re-signed fixtures

**The REAL defect is the missing half of the pipeline:** no step in
.github/workflows/release.yml ever runs scripts/release/sign_update_manifest.py, and no
signed manifest is attached to the release. docs/ARTIFACT_TRUST_CHAIN.md §3 claims
"release.yml order: ... → Sign update manifest (Ed25519 trust root)" — the doc is
ahead of the workflow; the signing step was never inserted.

### Live proof (probe /tmp/nexus_probe_release_sig.py against the REAL v9.0.11 release API):
```
live tag: v9.0.11 | assets: ['...-setup.exe','...-zip','CLI.exe','release-manifest.json','sbom.spdx.json','SHA256SUMS.txt']
PLAN STATUS: SECURITY_BLOCKED
signature_status: MANIFEST_MALFORMED
decision: signed update manifest REJECTED (MANIFEST_MALFORMED) — the signature is the trust root; refusing without it
```
CONSEQUENCE: every installed client 9.0.10/9.0.11 that runs `nexus update` refuses the
newest release — the auto-update path is bricked for all users. This is strictly worse
than the old unsigned state (fail-closed now = fail-often), and it is silent: release
runs are GREEN (34380430578 all jobs success) while producing uninstallable updates.

### Aggravating evidence
- gh secret list: only TELEGRAM_BOT_TOKEN/CHAT_ID/USER_ID — NSE_UPDATE_SIGNING_KEY was
  never configured, so even a wired step would fail the release at signing (the
  documented policy: fail rather than publish unsigned).
- Operator escrow path (docs §2): %USERPROFILE%\.nse-secrets\NSE_UPDATE_SIGNING_KEY.txt
  — not verifiable from this host; operator action required.
- Public key embedded: 8d6be6286be938b1fe0076129191791f8f596169d8e8a350c5fe844adc9e82ae
  (key_id 2026-09-root) — trust-root enforcement verified live (random key rejected).

## Fix (this run): wire the signing step into release.yml
Patch applied to .github/workflows/release.yml (+26 lines, no other file touched):
1. NEW step "Sign update manifest (Ed25519 trust root)" directly after
   "Embed release manifest in portable bundle", before verify-release:
   - env: NSE_UPDATE_SIGNING_KEY from secrets.NSE_UPDATE_SIGNING_KEY, NSE_VERSION from
     gates output, NSE_RELEASE_ID = github.run_id
   - runs scripts/release/sign_update_manifest.py "$NSE_OUT" "$NSE_OUT/NexusScalpEngine-$NSE_VERSION-win-x64.zip"
   - client-side re-verify with verify_manifest_signature (same call the clients make),
     asserting covered_digest == artifact_sha256
2. Upload step: added release/v*/windows/x64/manifests/update-manifest.signed.json to
   the artifact list (publish job already globs release-assets/**/*.json → the signed
   manifest lands as a release asset automatically).

## Verification (executable, not claimed)
1. YAML integrity: python3 yaml.safe_load on the patched release.yml → OK; step order
   verified programmatically: ... Checksums → Embed → **Sign update manifest** →
   verify-release → Code-signing gate → Upload.
2. E2E of the exact step command (tmp/nexus_e2e_sign_step.py) on the slim venv:
   - ran the REAL scripts/release/sign_update_manifest.py against a staged fake release
     tree (ephemeral seed injected via sitecustomize shim = same code path, throwaway key):
     `sign rc: 0 → UPDATE_MANIFEST_SIGNED artifact=... sha256=9ab598996927... key_id=2026-09-root`
   - client-side verify OK, payload binding OK (98304 bytes, digest match)
   - key hygiene OK: no seed material in the signed manifest
3. Existing trust-chain suites still green on slim venv:
   - tests/unit/test_signed_update_manifest.py → 16 passed
   - tests/unit/test_release_update_phase17.py → 64 passed

## BLOCKER (why this commit stays LOCAL, per workflow-scope contract)
The change touches .github/workflows/release.yml. The stored OAuth token CANNOT push
workflow-file changes (needs 'workflow' scope; pushes touching them are rejected).
Commit 2701fe49 is left on local main; the operator must push it manually or refresh
`gh auth` with workflow scope. **After pushing, the secret must also be configured
before the next tag** or the release correctly fails at the signing step:
  1. push the commit
  2. Settings → Secrets and variables → Actions → New secret:
     NSE_UPDATE_SIGNING_KEY = 64-hex escrowed seed (per docs/ARTIFACT_TRUST_CHAIN.md §2)
  3. verify: `gh secret list` shows NSE_UPDATE_SIGNING_KEY
  4. tag v9.0.12 (or re-tag) → release signs + publishes update-manifest.signed.json
  5. accept-check: the plan probe must flip from SECURITY_BLOCKED → UPDATE_AVAILABLE

## Ledger updates
- B5 status: PARTIAL (client verify: FIXED-VERIFIED; release signing wiring: fixed
  locally, push BLOCKED on workflow scope; secret: MISSING — operator action)
- roles 9-12 reminders unchanged; next role (9 Observability) may pick
  CRITICAL-COVERAGE web/* floors or the nightly-qa mkdir fix (chaos artifact path)

## Constraints honored
- no destructive git; foreign WIP untouched (26 dirty files from parallel agents remain)
- only .github/workflows/release.yml modified by this run (+25 lines via patch script)
- no cron jobs created/paused; no secrets printed (probe output contains public info only)
