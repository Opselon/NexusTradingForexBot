# Swarm Handoff Report: AGENT-QA — ML-QA-007

> **Task ID:** ML-QA-007 · **Owner:** AGENT-QA · **Priority:** P2
> **Stream:** STREAM L — CI/CD & Verification
> **Worktree:** `/tmp/wt-qa-parity`
> **Branch:** `agent/qa/parity-latency-determinism`
> **Base:** `52aaa8b0` (origin/main, 2026-09-23)
> **Census context:** Roster candidate #4 (`docs/ml-system/test_determinism_roster.md` §6)

---

## 1. Executive Summary

`tests/integration/test_mt5_adapter_parity.py` was ranked **#4** in the
ML-QA-003 determinism census: it carried 6 `time.perf_counter()` probes inside
latency *measurement* asserts, is in the push-gate manifest
(`tests/critical_suite.txt` line 264), and executes on the cross-OS matrix
(`tests-os.yml` FULL lane on windows-latest + macos-latest for every main push,
Monday schedule, and dispatch).

A wall-clock p99 under `-n auto --dist loadgroup` saturation measures the
scheduler, not the serialization cost. That exact failure shape forced commit
`9279f1ea` to *drop* the absolute 1ms threshold from the two order-serialization
SLA tests, leaving the contract weakened.

This task remediates all three latency tests (test-only; zero production code,
zero adapter-contract change):

1. **CPU time, not wall clock:** probes moved from `time.perf_counter()` to
   `time.process_time()`, which excludes co-tenant runner preemption by
   construction.
2. **Warmup before timing:** a 5-iteration warmup loop precedes the timed
   window in both SLA tests, with `calls.clear()` so the `len(calls) == 1000`
   invariant is honest.
3. **SLA re-attached:** the <1ms serialization SLA is re-asserted on CPU time
   as `mean_cpu_ms < 1.0` — the contract is measured again instead of abandoned.
4. **Shared helper:** the loopback round-trip bound moved onto
   `tests.e2e.chain_clock.budget_cpu_ms(500.0)` (one measurement
   implementation tree-wide, inherited from ML-QA-004).
5. **Contract pinned:** 19-test regression battery
   `tests/unit/test_ml_qa_007_parity_latency_determinism.py`, registered in
   `tests/critical_suite.txt` (239 paths) so it rides the required
   Code Quality & Tests / Pytest check — keeping the CHG-0049 gate-parity
   contract honest (no CI-only gate class, no workflow file touched).
6. **Roster evidence:** §6 candidate #4 annotated REMEDIATED with a pointer to
   the contract battery and the negative-control result.

---

## 2. Evidence of Verification

- `pytest tests/integration/test_mt5_adapter_parity.py`: **49/49 passed** in
  12.39s (baseline at origin/main `52aaa8b0` was also 49 passed — zero
  behavioral regression).
- `pytest tests/unit/test_ml_qa_007_parity_latency_determinism.py`:
  **19/19 passed** in 0.10s.
- `pytest tests/unit/test_ml_qa_004_determinism_remediation.py`:
  **24/24 passed** in 0.09s (the prior remediation's regression guard is
  untouched and still green).
- Combined slice (parity + ML-QA-007 + ML-QA-004 + merge-marker residue):
  **112 passed** in 17.44s.
- **Negative control** (pre-remediation module text at `52aaa8b0`): the
  contract battery's invariants provably fail — 3 bare
  `t0 = time.perf_counter()` anchors, zero warmup loops before the first timed
  probe, no `calls.clear()`. The tests are live, not tautological.
- **Gates:** ruff check clean, ruff format clean, mypy Success (2 source
  files), `verify_critical_suite_manifest.py` CRITICAL_SUITE_MANIFEST_OK
  (238 paths at this base), `gate_parity.py` PASS,
  `check_ml_contract_drift.py` clean, `check_merge_marker_residue.py` clean
  (3457 files).
- `check_local.py --all --fast`: all stages pass except `fast_tests`, whose
  only failure is `tests/unit/test_capital_protection_a15.py` with
  `ModuleNotFoundError: No module named 'torch'`
  (`src/nexus_scalp/application/live_engine.py:37`) — the **proven
  pre-existing** torch-absent environment red, identical on the pristine
  shared main tree; 22/22 green under the torch venv
  (`PYTHONPATH=src:.:/tmp/nse-slim/lib/python3.11/site-packages
  /tmp/nse-venv/bin/python`). Not caused by this change.

---

## 3. Files

| File | Change |
|---|---|
| `tests/integration/test_mt5_adapter_parity.py` | 3 latency tests remediated (CPU-time probes, warmup, `calls.clear()`, mean SLA bound, `budget_cpu_ms`); header docstring + `budget_cpu_ms` import |
| `tests/unit/test_ml_qa_007_parity_latency_determinism.py` | NEW — 19-test contract battery |
| `tests/critical_suite.txt` | +1 manifest entry (239) |
| `docs/ml-system/test_determinism_roster.md` | §6 candidate #4 annotated REMEDIATED |
| `docs/ml-system/tasks/ML-QA-007.md` | NEW — task definition, DONE |
| `docs/ml-system/TASK_BOARD.md` | +1 ML-QA-007 row, DONE |
| `docs/ml-system/06_TASK_LEDGER.md` | +1 ML-QA-007 row, DONE |
| `agents/taskboard.md` | +1 row, DONE |

**Not touched:** `RemoteMT5GatewayAdapter` and its client contract (frozen);
`.github/workflows/*` (hard constraint); any production source under `src/`.
The `time.sleep(latency_seconds)` harness knob is a *stimulus*, not a
measurement, and was left alone.

---

## 4. Swarm Continuation Note

`ML-QA-006` (PR #409, all 15 checks SUCCESS + 4 conditional skips, MERGEABLE)
adds its own ledger/taskboard rows from its branch; this branch does **not**
duplicate them — a second `ML-QA-006` row in the ledger would trip the
duplicate-row detector that task itself delivered. Once #409 lands, the two
branches' metadata rows coexist without conflict (different ids, different
files' line ranges).
