# 2026-09-24 — AGENT-QA — ML-QA-009 — Audit-Flush Bounded-Wait Determinism (roster #7)

Run: swarm cycle 42 (2h). Branch `agent/qa/ml-qa-009` off cbabadae.
Role: AGENT-QA. Status: DONE-PR-OPENED.

## What
`tests/unit/test_audit_flush_contract.py::test_flush_returns_false_when_worker_stalled`
asserted `elapsed < 2.0` on `time.monotonic()` around a deliberately wedged
`flush(timeout_sec=0.2)` — a wall-clock magnitude assert in the push gate
(critical_suite line 288), the roster §3 "Timing assert" flake class.
Head recount: the census's "4 monotonic probes" was stale — ML-QA-004 had
already converted the idle-flush bound to `budget_cpu_ms`; exactly ONE probe
pair (2 calls) remained.

## Fix (test-only)
Both legs -> shared `tests/e2e/chain_clock.py::budget_cpu_ms(2000.0)` (CPU
time); `assert ok is False` (the real boundedness contract: flush returned,
no deadlock) kept hard; `timeout_sec=0.2` production deadline path, unwedge
and `close()` teardown kept; dead `import time` removed. Rationale comment in
the test documents why the wall assert is gone: a hang is caught by the CI
test timeout, never by a wall-clock magnitude.

## Contract battery
`tests/unit/test_ml_qa_009_audit_flush_cpu_budget.py` (8 tests, textual,
slim venv, no sqlite/torch import), registered in `tests/critical_suite.txt`
(240 paths OK, manifest line 492).

## Evidence (all local, /tmp/nse-slim, py3.11.16)
- module + battery: **14 passed** (2.56s, `-rA` individual PASSED list)
- NEGATIVE CONTROL: pre-remediation text -> battery **3 FAILED / 5 passed**
  rc=1; fixed text -> 8/8. Live, not tautological.
- ruff check + format --check clean; mypy Success (2 files)
- manifest 240 OK; merge-marker clean (3525 files); duplicate-rows OK
  (2 tables); gate_parity drifts=0; dependency-drift OK (99 pins)

## Also this run (PR #411 repair)
PR #411 (ML-QA-007) was CONFLICTING/DIRTY vs moved main: 3 metadata files
(agents/taskboard.md, 06_TASK_LEDGER.md, TASK_BOARD.md). Merged origin/main
in `/tmp/wt-qa-parity`, kept BOTH sides' rows (ML-QA-007 + ML-QA-008 +
EUP-RELEASE-001), stripped the leftover `>>>>>>>` close marker (caught by
check_merge_marker_residue itself), gates re-run green (marker clean,
dup-rows OK, manifest 240, 113-test slice PASSED, ruff/format clean,
depdrift 99 pins), pushed `bf4c766f`; merge-tree vs fresh origin/main rc=0.

## Not touched
No workflow files, no production src, no adapter contract. Roster #8 stays
deferred (skipif on data file absent from git).
