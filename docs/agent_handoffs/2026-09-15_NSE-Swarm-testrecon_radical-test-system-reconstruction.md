# Handoff — NSE Radical Test System Reconstruction (2026-09-14/15)

**Agent:** NSE-Swarm main-mind (test-recon lane) · **Branch:**
`nse/radical-test-reconstruction` (from `origin/main@19a12151`) ·
**Status:** COMPLETE — awaiting PR review.

## What was done (one paragraph)

Forensic census of all 481 test files / 5,810 tests (AST inventory + measured
xdist wall-times + 4 read-only forensic sub-lanes covering unit A/B, non-unit,
and CI workflows), then evidence-driven reconstruction: Tier 0-4 manifests
wired to real jobs, 3 measured cost outliers demoted T1→T2 (which now actually
runs on main push + release SHA), 29 P0-safety tests promoted into the PR gate,
5 new mutation-validated batteries for verified zero-test gaps, 5 files deleted
(mock-noop / CI-skip-only / dead-functionality — each justified in
`docs/testing/test_value_matrix.md`), `tests-os.yml` PR lane reduced from a
third full-suite re-run to a <60-worker-second Windows skew subset with an
always-report honesty contract (required contexts unchanged), release.yml gates
duplication removed. **Measured PR-critical suite: 662s → 127s wall** (local
8-core xdist), 1851 tests. Branch protection and release-auth required-check
lists were deliberately NOT changed.

## Files (authority map)

- `docs/testing/TEST_ARCHITECTURE.md` — canonical tier contract
- `docs/testing/critical_regressions.md` — CR-001..036 registry; CONSULT BEFORE
  deleting/demoting any listed test
- `docs/testing/test_inventory.json` + `test_value_matrix.md` — per-file scores
  (generated by `scripts/testing/build_inventory.py`)
- `docs/testing/CI_COST_REPORT.md` — measured before/after, provenance-marked
- `scripts/testing/mutation_check.py` — 9/9 KILLED TDD evidence, rerunnable
- `tests/critical_suite.txt` (T1), `tests/extended_suite.txt` (T2, NEW),
  `tests/windows_skew_suite.txt` (T3, NEW), slow/fast unchanged in role

## Invariants held

INV-001..012 untouched (no src/ production change — `git diff src/` is empty by
design; §32 no test-only production code). No gate was weakened: zero skips,
zero xfails added, zero required contexts removed; the 3 demoted files RUN
MORE than before (they previously had no main-push lane at all).

## Known-open (do not treat as regressions)

1. `test_smoke_self::test_full_smoke_under_budget` — 37 checks < 40 locally,
   GREEN on CI ubuntu; environment-tiered check complement, pre-existing in the
   BEFORE baseline. Fix = make the smoke complement environment-independent.
2. `test_packaged_db_and_mode_bug146_149` — promotion REVERTED after
   measurement (order-dependent vs LEGACY_UNVERIFIED machine artifact);
   rewrite onto the bug269 provisioned bundle, then re-promote.
3. 396/481 files UNREVIEWED (subagent-report salvage limit): never delete an
   UNREVIEWED file; next wave = rerun forensics lanes into
   `scripts/testing/classification_overrides.json`.
4. 24 baseline unit-red files (order/env dependence; census
   `scratch/…/junit_unit_before.xml` on the reconstruction machine) were NOT
   touched — several are on the deferred merge/rewrite list.
5. If protection is ever edited (macOS context etc.), sync
   `release_auth_gate.REQUIRED_MAIN_CI_CHECKS` +
   `artifacts/forensics/branch_protection_required_checks.json` atomically —
   the drift test enforces it.

## Reproduce the evidence

```
.venv/Scripts/python.exe scripts/testing/inventory_tests.py --out scratch/recon/inventory_raw.json
.venv/Scripts/python.exe scripts/testing/mutation_check.py            # 9/9 KILLED
pytest $(grep -v '^#' tests/critical_suite.txt | grep -v '^$') -n auto --dist loadgroup   # ~127s local
scripts/ci/check_local.py                                             # local mirror
```
