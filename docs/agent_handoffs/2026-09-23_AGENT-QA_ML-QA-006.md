# 2026-09-23 · AGENT-QA · ML-QA-006 — Duplicate Canonical Task-Row Detector

**Run:** 39 (NSE Autonomous ML Specialist Swarm) · **Role:** AGENT-QA · **Status:** DONE-PR-OPENED
**Task:** ML-QA-006 (new QA-lane task, companion to ML-QA-005) · **Dep:** ML-QA-005 (DONE, PR #405)

## What was delivered

1. `scripts/ci/check_duplicate_task_rows.py` (new, stdlib-only):
   Structural gate over the shared SSOT metadata tables. Fails (exit 1) when
   more than one canonical table row carries the same task id in a declared
   table. Declared tables:
   - `docs/ml-system/06_TASK_LEDGER.md` — section `## 2. Master Backlog Table`
     only (the §1 Historical Migration Map legitimately pairs legacy+new ids
     on one row and is excluded by the section scope);
   - `docs/ml-system/TASK_BOARD.md` — whole file.
   Canonical row identity = the LEADING task id in the first table cell;
   dependency-cell mentions of other ids are not rows (the ledger's
   Dependencies column restates other ids on every row by design — counting
   them would make the gate 100% false-positive). Backticked `` `ML-X-1` ``
   and plain `ML-X-1` are the same identity. Exit 0/1/2 = clean/defect/
   config-error. `--json` emits one unwrapped stdout line (BUG-300 lesson).
   `--root` points the gate at a sandbox for tests.
2. `tests/unit/test_duplicate_task_rows.py` (new, 10 tests) — real tables
   clean at HEAD (plain + JSON), duplicate negative control, the exact
   PR #347 3-contradictory-rows shape, dependency-mention false-positive pin,
   migration-map section scoping pin, backtick/plain identity, stdlib-only
   import purity, missing-root rc=2, one-line JSON purity.
3. Gate parity both sides (CHG-0049): test registered in
   `tests/critical_suite.txt` (238 paths) so the required
   Code Quality & Tests / Pytest check runs the gate over the committed
   tree; `scripts/ci/check_local.py` gained stage `[5d] duplicate_task_rows`
   for the local pre-push gate. No workflow file edited (HARD CONSTRAINTS).
4. Docs closeout: `docs/ml-system/tasks/ML-QA-006.md` (new),
   ledger row + ML-QA-005 status refresh (PR #405 / `a1a798f2`),
   `TASK_BOARD.md` bullet, `agents/taskboard.md` row.

## Why this task

ML-QA-005's marker guard caught the diff3 residue of PR #347, but the
defect class it exposed — three contradictory rows for one task id
(`ML-CI-002`: DONE / BLOCKED / DONE-with-wrong-owner) committed as literal
table rows — is structural, not marker-shaped. Every agent PR rewrites the
shared metadata tables from its own snapshot; a stale rebase can commit
duplicates without any conflict marker. No existing gate looked at table
structure. This closes that class permanently and standalone.

## Verification (branch tip, Python 3.11.16, slim venv)

- `pytest tests/unit/test_duplicate_task_rows.py` → **10/10 pass** (0.94 s)
- combined regression slice `test_duplicate_task_rows +
  test_merge_marker_residue + test_ml_qa_004_determinism_remediation`
  → **54 passed**
- **NEGATIVE CONTROL**: injected the exact duplicate `ML-CI-002` row into
  the real ledger at lines 82-83 → gate printed
  `ML-CI-002 rows at lines [82, 83]`, exit 1; `git checkout --` restore →
  clean exit 0
- both declared tables scan **CLEAN at HEAD** (no duplicates exist today —
  the ML-QA-005 repair already deduped the incident rows)
- `check_local.py --all --fast`: every stage passed except `fast_tests`,
  whose only failures are the **pre-existing torch-absent red**
  (`ModuleNotFoundError: No module named 'torch'` at
  `src/nexus_scalp/position_adviser/service.py:35`, reproduces identically
  on the pristine main shared tree at `52aaa8b0`); 22/22 pass under the
  torch venv (`/tmp/nse-venv/bin/python`)
- `ruff check` + `ruff format --check` clean (3 touched files)
- `mypy` → Success, no issues (2 source files)
- `verify_critical_suite_manifest.py` → CRITICAL_SUITE_MANIFEST_OK, 238 paths
- `check_merge_marker_residue.py` → clean (3457 files, 757.7 ms)
- `gate_parity.py --json` → PASS; `check_ml_contract_drift.py` → clean;
  `check_docs.py` → DOCS_HEALTH = PASS

## Notes for the next cycle

Board state: every ML-XXX task is DONE or HUMAN_DECISION_REQUIRED
(ML-ARCH-001, ML-GOV-002, ML-FEAT-003, ML-EXP-002, ML-OBS-002).
Open agent PRs at run start: #406 (ML-QA-003 ledger repair, docs-only),
#408 (DB-FABRIC-001, another lane).
