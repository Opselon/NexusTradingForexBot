"""Live-engine decomposition package (P1 seam L1+).

Extracted live-engine responsibilities (shadow recording, and later
pipeline/bar/model-bundle components) live here; the LiveEngine facade
in application/live_engine.py composes them.
"""
