execution:
  symbol: "EURUSD"
  mode: "PAPER"
  timeframe: "M1"
  magic_number: 999101
  max_slippage_points: 10

risk:
  max_account_drawdown_pct: 2.5
  risk_per_trade_pct: 0.5
  max_concurrent_positions: 1
  max_spread_points: 20
  enforce_stop_loss: true

mt5:
  timeout_ms: 5000
  retries: 3
  portable_mode: false

model:
  confidence_threshold: 0.75
  feature_schema_version: "v1.0"
  model_artifact_path: "artifacts/models/scalp/EURUSD/v1.0.0/model.pt"

algo:
  atr_sl_buffer_multiplier: 1.5
  min_risk_reward_ratio: 1.8
  ai_zone_confidence_threshold: 0.60
  fvg_mitigation_sensitivity: 0.5
  order_block_lookback_bars: 30

candle_intel:
  enabled: true
  entry_min_confidence: 0.62
  strong_body_ratio: 0.60
  weak_close_blocks_entry: true
  db_path: "artifacts/candle_intel.db"

news:
  enabled: true
  db_path: "artifacts/news.db"
  worker_interval_sec: 60
  polling:
    fast_interval_sec: 180
    medium_interval_sec: 600
    slow_interval_sec: 3600
  analysis:
    mode: "HYBRID"
    provider: ""
    api_base_url: ""
    model: ""
    max_api_per_cycle: 5
    api_importance_floor: 0.55
    request_timeout_sec: 20.0
    enabled: true
