🤖 *استراتژی فکتوری — اتصال زنده هوش مصنوعی فعال شد*

✅ *API متصل و تأیید شد*
• آدرس: http://178.105.20.69:20128/v1
• مدل: claude-opus-5 (پاسخ JSON دریافت شد)
• کلید API به‌صورت رمزنگاری‌شده (DPAPI) در Secret Store ذخیره شد

🔧 *بخش‌های پیاده‌سازی‌شده*
1) تنظیمات از پنل وب: کلید API، آدرس، مدل و دما قابل تغییر (بدون ریستارت)
2) پرامپت قوی v2: کاتالوگ ۷۰ ویژگی دقیقاً از schema_contract، ممنوعیت داده آینده، سقف پیچیدگی
3) تجزیه امن JSON حتی با داده اضافی SSE سرور
4) ایزوله‌سازی: هر کاندیدای LLM از دروازه‌های اعتبارسنجی قطعی عبور می‌کند
5) ذخیره‌سازی سازگار: SQLite ✅ + PostgreSQL واقعی ✅

📊 *آزمون‌ها*
• 40 تست فکتوری: همه سبز
• تست رمزنگاری کلید در حالت استراحت: تأیید
• تست PostgreSQL واقعی (کانتینر محلی): تأیید
• قیمت برآوردی هر درخواست: ~$0.01 (گزارش هزینه در UI)

🔄 *چرخه استراتژی*
GENERATE → VALIDATE → BACKTEST → WALK-FORWARD → OOS → ROBUSTNESS → SCORE → RANK → ELITE → EVOLVE
هیچ کاندیدایی به‌صورت خودکار وارد معامله نمی‌شود

کامیت‌ها: 6e33bfa + e743414 (پوش شدند)
